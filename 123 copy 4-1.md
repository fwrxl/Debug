{
  "meta": {
    "exported_at": "2026-05-22T15:33:44.492877",
    "created_at": "2026-05-22T14:49:51.022812"
  },
  "global_iteration": [
    {
      "step": "situation_analysis",
      "evidence": {
        "possible_modules": [
          {
            "module": "intent_classifier",
            "explanation": "意图分类器的裁决逻辑存在矛盾：rule_fast 和 llm_cmd 都命中指令（intent=1，conf=0.8/0.7），但最终裁决结果却是 intent=2（闲聊，conf=0.42）。这种内部矛盾导致系统将用户的移动指令误判为闲聊，是后续所有异常的根本原因。",
            "confidence": 0.88
          },
          {
            "module": "parameter_extractor",
            "explanation": """regex="往<方向>走" 无法匹配用户输入"往前走"，导致参数提取失败并回退到默认"前"。但需注意：如果上游意图分类器正确识别为指令（intent=1），参数提取器可能采用不同的提取策略或正则模式，因此当前 regex 匹配失败可能是被上游意图误判污染后的症状表现，自身缺陷置信度中等。""",
            "confidence": 0.45
          },
          {
            "module": "protocol_builder",
            "explanation": """协议生成器成功找到 template="move_forward" 并生成了协议。但设备最终无响应，可能是因为收到的 MQTT 消息内容存在问题（如参数错误导致协议内容不符合设备预期）。协议生成器的输出质量取决于上游参数提取器的准确性。""",
            "confidence": 0.35
          },
          {
            "module": "emqx_client",
            "explanation": "MQTT 客户端出现超时：重试耗尽，设备无响应。但这是最下游模块，其表现异常是上游模块（包括意图分类、参数提取、协议生成）错误累积传导导致的症状。MQTT 超时不一定是 emqx_client 本身配置或代码缺陷，更可能是它发送的消息内容或 topic 本身就有问题。",
            "confidence": 0.22
          }
        ]
      },
      "success": true,
      "target_module": "N/A",
      "confidence": 0.0,
      "timestamp": "2026-05-22T14:50:39.593129"
    },
    {
      "step": "global_reflection",
      "evidence": {
        "root_cause_module": "intent_classifier",
        "confidence": 0.95,
        "explanation": "根因：intent_classifier 的裁决组件将用户指令'往前走'误判为闲聊(intent=2)，置信度仅0.42，日志明确标注'裁决失败'。llm_light(intent=1, conf=0.65)和llm_cmd(intent=1, conf=0.7)两个模型均明确识别为指令，但裁决结果却输出闲聊intent=2，rule_fast的误判覆盖了多模型共识。该错误发生在Pipeline最前端(阶段1)，直接导致后续所有模块处理的是闲聊意图而非移动指令。因果链验证：instruction_rewriter接收到的输入已被错误分类为闲聊而非指令；parameter_extractor的正则匹配失败和protocol_builder基于错误意图的协议生成均为该根因的连锁症状——它们接收到的输入已被上游错误污染；emqx_client的设备无响应是最终表现而非独立根因。关键证据：1) 日志'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'；2) 日志'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令'明确标注裁决矛盾；3) 分支reflection显示intent_classifier confidence=0.95。",
        "symptom_modules": [
          "instruction_rewriter",
          "parameter_extractor",
          "protocol_builder",
          "emqx_client"
        ]
      },
      "success": true,
      "target_module": "intent_classifier",
      "confidence": 0.95,
      "timestamp": "2026-05-22T15:33:43.533772"
    }
  ],
  "possible_modules": [
    {
      "module": "intent_classifier",
      "confidence": 2.7800000000000002,
      "explanation": "意图分类器的裁决逻辑存在矛盾：rule_fast 和 llm_cmd 都命中指令（intent=1，conf=0.8/0.7），但最终裁决结果却是 intent=2（闲聊，conf=0.42）。这种内部矛盾导致系统将用户的移动指令误判为闲聊，是后续所有异常的根本原因。"
    },
    {
      "module": "parameter_extractor",
      "confidence": 0.5700000000000001,
      "explanation": """regex="往<方向>走" 无法匹配用户输入"往前走"，导致参数提取失败并回退到默认"前"。但需注意：如果上游意图分类器正确识别为指令（intent=1），参数提取器可能采用不同的提取策略或正则模式，因此当前 regex 匹配失败可能是被上游意图误判污染后的症状表现，自身缺陷置信度中等。"""
    },
    {
      "module": "protocol_builder",
      "confidence": 0.35,
      "explanation": """协议生成器成功找到 template="move_forward" 并生成了协议。但设备最终无响应，可能是因为收到的 MQTT 消息内容存在问题（如参数错误导致协议内容不符合设备预期）。协议生成器的输出质量取决于上游参数提取器的准确性。"""
    },
    {
      "module": "emqx_client",
      "confidence": 0.24,
      "explanation": "MQTT 客户端出现超时：重试耗尽，设备无响应。但这是最下游模块，其表现异常是上游模块（包括意图分类、参数提取、协议生成）错误累积传导导致的症状。MQTT 超时不一定是 emqx_client 本身配置或代码缺陷，更可能是它发送的消息内容或 topic 本身就有问题。"
    }
  ],
  "global_context": [],
  "branch_memories": {
    "intent_classifier": {
      "context": [
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
尚未执行任何 capability

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T14:51:15.887000"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "阶段1日志已暴露明确的裁决矛盾：rule_fast和llm_cmd都命中指令（intent=1），但最终裁决却选择intent=2（闲聊），conf=0.42。这种裁决算法在多个子模块明确支持指令时仍选择闲聊的逻辑矛盾，属于模块内部裁决缺陷，需要通过log_localization验证裁决逻辑的具体问题点"
}
```""",
          "timestamp": "2026-05-22T14:51:15.887015"
        },
        {
          "step": "log_localization_llm",
          "role": "user",
          "content": """## 任务

你是机器人控制 Pipeline 的日志分析专家。验证可疑模块是否存在问题，定位到具体的组件。

## 待验证模块

模块名称: intent_classifier
模块置信度: 0.0
怀疑原因: 

模块内部组件: classify, ensemble, 裁决, conf_threshold

**你必须只分析这个模块，不要分析其他模块。**
**通过日志验证该模块是否确实存在问题，并定位到具体的组件。**

## 当前分支记忆

## 上下文

- [llm_decision] assistant: ```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "阶段1日志已暴露明确的裁决矛盾：rule_fast和llm_cmd都命中指令（intent=1），但最终裁决却选择intent=2（闲聊），conf=0.42。这种裁决算法在多个子模块明确支持指令时仍选择闲聊的逻辑矛盾，属于模块内部裁决缺陷，需要通过log_localization验证裁决逻辑的具体问题点"
}
```


## 输入信息
情景描述: 用户说往前走但机器人没有反应
失败日志:

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 分析要求

1. **只分析上述待验证模块**：不要分析其他模块，即使日志中提到了其他模块
2. **分析该模块的日志表现**：查看该模块在日志中的输出
3. **定位问题组件**：找出该模块中具体哪个组件出了问题
4. **如果该模块没有问题**：返回 explanation = "verified_no_issue"
5. **计算置信度**：根据日志证据的充分性和问题定位的确定性，给出 confidence (0.0~1.0)

## 输出格式

必须返回纯 JSON 对象，禁止包含任何 Markdown 标记、思考过程或解释文字：

{
  "failed_module": "intent_classifier",
  "failed_component": "该模块内的具体组件名称",
  "error_location": "日志中的具体错误位置",
  "explanation": "根本原因分析，或 'verified_no_issue' 表示该模块没有问题",
  "evidence": ["日志证据1", "日志证据2"],
  "confidence": 确认是当前模块出问题的置信度，0.0~1.0 的数字。
}

## Few-Shot 示例

### 示例 1：模块自身存在问题（独立根因）
输入：待验证模块=intent_classifier，模块组件=["classify", "ensemble", "裁决", "conf_threshold"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
[Ensemble] trace rule_fast: intent=2 conf=0.8
[Ensemble] trace llm_cmd: intent=1 conf=0.7
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
```
输出：
{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件在 rule_fast(0.8)和 llm_cmd(0.7)都识别为指令时仍输出闲聊 intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'"
  ],
  "confidence": 0.9
}

### 示例 2：模块没有问题
输入：待验证模块=command_store，模块组件=["vector_search", "text_search", "merge_results"]
日志：
```
[CommandStoreClient] vector_search k=5 score=0.72
[CommandStoreClient] matched_command: {"id": "cmd_001"}
✅ ES搜索成功: 匹配 cmd_001
```
输出：
{
  "failed_module": "command_store",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'"
  ],
  "confidence": 0.0
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入：待验证模块=parameter_extractor，模块组件=["regex_match", "capture", "default_fallback"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"
```
输出：
{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex="往<方向>走" no match in "往前走"",
  "explanation": "regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。当前模块接收到的输入已被上游错误污染（'往前走'被误判为闲聊意图，导致 regex 模式不匹配）。这是上游错误导致的症状，不是 parameter_extractor 的独立根因。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游意图分类已误判",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 当前模块收到异常输入 → regex 匹配失败是症状而非独立缺陷"
  ],
  "confidence": 0.2
}

**重要**：示例3 中 confidence 为 0.2（低分），因为虽然当前模块确实出现了异常，但其异常可以被更上游的错误完全解释。根因是上游的 intent_classifier，不是当前模块。

## confidence 语义（必须严格遵守）

confidence 表示"**日志证据支持'问题根因在该模块'的置信度**"，不是"该模块工作质量评分"。

- 模块**确实有问题** → confidence 越高表示越确信（0.5~1.0）
- 模块**没有问题**（explanation = "verified_no_issue"）→ confidence **必须为 0.0**，表示"问题在该模块"的概率为零

严禁出现 explanation="verified_no_issue" 但 confidence > 0 的矛盾输出。

## 重要约束

1. **只输出 JSON**：禁止输出 ```json、```、解释文字或思考过程
2. **failed_module**：必须返回 "intent_classifier"
3. **explanation**：如果该模块没有问题，必须返回 "verified_no_issue"，**此时 confidence 必须为 0.0**
4. **confidence**：必须返回 0.0~1.0 的数字，语义是"问题根因在该模块"的置信度
5. **evidence**：必须列出支撑 explanation 的具体日志证据，不能为空
5. **只分析待验证模块**：不要分析其他模块
6. **所有字段必须完整**：输出必须包含全部 5 个字段，缺一不可""",
          "timestamp": "2026-05-22T14:52:08.027785"
        },
        {
          "step": "log_localization_llm",
          "role": "assistant",
          "content": """{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42 和 ⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令",
  "explanation": "裁决组件存在逻辑缺陷：llm_light(intent=1, conf=0.65)和llm_cmd(intent=1, conf=0.7)两个子模块都明确识别为指令，且总置信度(1.35)高于rule_fast单独识别为闲聊(conf=0.8)。但裁决算法最终输出intent=2(闲聊)，conf=0.42。这种在多数子模块支持指令时仍选择闲聊的决策违背了ensemble投票逻辑，是裁决算法的独立根因问题。",
  "evidence": [
    "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms' - llm_light明确识别为指令",
    "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms' - llm_cmd明确识别为指令",
    "日志行：'[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms' - rule_fast识别为闲聊但仅因chat_pattern",
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 最终裁决错误选择闲聊intent=2",
    "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决矛盾"
  ],
  "confidence": 0.85
}""",
          "timestamp": "2026-05-22T14:52:08.027806"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T14:52:12.105516"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """```json
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志显示 intent_classifier 裁决结果与 rule_fast(0.8) 和 llm_cmd(0.7) 两个子模块明显矛盾，将 '往前走' 误判为 intent=2(闲聊)。裁决矛盾是症状性异常，需要深入代码逻辑查看 ensemble 裁决规则、权重配置、以及为何高置信度子模块被覆盖，才能定位根因"
}""",
          "timestamp": "2026-05-22T14:52:12.105530"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization, code_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:09:25.450884"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{"capability":"code_localization","end_signal":false,"reason":"已执行过 log_localization，但日志未暴露代码级根因。当前核心问题是：用户明确说“往前走”，3个模型中2个认为是指令(llm_light=0.65, llm_cmd=0.7)，但最终裁决却分类为闲聊(intent=2)。需要深入 intent_classifier 的集成裁决代码，检查其权重配置、规则冲突处理逻辑为何做出与多数模型相反的判断"}""",
          "timestamp": "2026-05-22T15:09:25.450890"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization, code_localization, code_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:25:17.197912"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过 log_localization 和 code_localization，且 code_localization 最多执行一次。intent_classifier 的置信度 0.6 表示当前模块很可能是问题根因（裁决逻辑存在矛盾：rule_fast+llm_cmd 都指向指令 intent=1，但最终裁决为 intent=2 闲聊），但当前分支的定位能力已耗尽，应进入 Reflection 进行综合判断和最终决策"
}""",
          "timestamp": "2026-05-22T15:25:17.197930"
        },
        {
          "step": "reflection_llm",
          "role": "user",
          "content": """## 任务

你是当前诊断分支的反思器。你的唯一任务是：基于分支内已执行的能力结果和失败日志，评估"问题根因是否属于当前分支的模块 intent_classifier"。

你禁止执行任何其他判断，包括但不限于：
- 禁止决定是否发送通知
- 禁止决定整个诊断流程是否停止
- 禁止将其他模块指定为问题模块
- 禁止输出任何与 JSON 格式无关的思考过程或解释文字

## 当前分支模块

intent_classifier

## 情景描述

用户说往前走但机器人没有反应

## 失败日志


# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 当前分支完整记忆（包含决策对话和能力执行结果）

## 上下文

- [llm_decision] assistant: ```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "阶段1日志已暴露明确的裁决矛盾：rule_fast和llm_cmd都命中指令（intent=1），但最终裁决却选择intent=2（闲聊），conf=0.42。这种裁决算法在多个子模块明确支持指令时仍选择闲聊的逻辑矛盾，属于模块内部裁决缺陷，需要通过log_localization验证裁决逻辑的具体问题点"
}
```
- [log_localization_llm] assistant: {
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42 和 ⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令",
  "explanation": "裁决组件存在逻辑缺陷：llm_light(intent=1, conf=0.65)和llm_cmd(intent=1, conf=0.7)两个子模块都明确识别为指令，且总置信度(1.35)高于rule_fast单独识别为闲聊(conf=0.8)。但裁决算法最终输出intent=2(闲聊)，conf=0.42。这种在多数子模块支持指令时仍选择闲聊的决策违背了ensemble投票逻辑，是裁决算法的独立根因问题。",
  "evidence": [
    "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms' - llm_light明确识别为指令",
    "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms' - llm_cmd明确识别为指令",
    "日志行：'[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms' - rule_fast识别为闲聊但仅因chat_pattern",
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 最终裁决错误选择闲聊intent=2",
    "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决矛盾"
  ],
  "confidence": 0.85
}
- [llm_decision] assistant: ```json
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志显示 intent_classifier 裁决结果与 rule_fast(0.8) 和 llm_cmd(0.7) 两个子模块明显矛盾，将 '往前走' 误判为 intent=2(闲聊)。裁决矛盾是症状性异常，需要深入代码逻辑查看 ensemble 裁决规则、权重配置、以及为何高置信度子模块被覆盖，才能定位根因"
}
- [llm_decision] assistant: {"capability":"code_localization","end_signal":false,"reason":"已执行过 log_localization，但日志未暴露代码级根因。当前核心问题是：用户明确说“往前走”，3个模型中2个认为是指令(llm_light=0.65, llm_cmd=0.7)，但最终裁决却分类为闲聊(intent=2)。需要深入 intent_classifier 的集成裁决代码，检查其权重配置、规则冲突处理逻辑为何做出与多数模型相反的判断"}
- [llm_decision] assistant: {
  "capability": null,
  "end_signal": true,
  "reason": "已执行过 log_localization 和 code_localization，且 code_localization 最多执行一次。intent_classifier 的置信度 0.6 表示当前模块很可能是问题根因（裁决逻辑存在矛盾：rule_fast+llm_cmd 都指向指令 intent=1，但最终裁决为 intent=2 闲聊），但当前分支的定位能力已耗尽，应进入 Reflection 进行综合判断和最终决策"
}

## 能力使用

- ✅ log_localization (confidence=0.85)
  failed_module: intent_classifier
  explanation: 裁决组件存在逻辑缺陷：llm_light(intent=1, conf=0.65)和llm_cmd(intent=1, conf=0.7)两个子模块都明确识别为指令，且总置信度(1.35)高于rule_fast单独识别为闲聊(conf=0.8)。但裁决算法最终输出intent=2(闲聊)，conf=0.42。这种在多数子模块支持指令时仍选择闲聊的决策违背了ensemble投票逻辑，是裁决算法的独立根因问题。
- ✅ code_localization (confidence=0.92)
  module: intent_classifier
  explanation: ensemble.py 的 _weighted_vote 使用乘法融合公式 score=Π(1+w*conf)，且 rule_fast 硬编码权重为 1.0（最高权重）。当日志显示 rule_fast 识别为闲聊(conf=0.8)而 llm_light(intent=1,conf=0.65)和 llm_cmd(intent=1,conf=0.7)都识别为指令时，乘法融合允许单个高权重投票者覆盖多个低权重投票者的多数意见，导致最终裁决错误选择闲聊(intent=2,conf=0.42)。此外 classify() 中 rule_fast 的短路返回逻辑与日志显示的 ensemble 投票行为存在矛盾，表明该模块的裁决设计存在根本性缺陷。
- ✅ code_localization (confidence=0.92)
  module: intent_classifier
  explanation: intent_classifier 模块的 ensemble 裁决存在根本性设计缺陷，与日志显示的裁决矛盾高度吻合。核心问题包括：1) _weighted_vote 使用乘法融合公式 score=Π(1+w*conf)，初始值 1.0 不公平地放大了未获投票意图的基数优势，允许单个高权重投票者覆盖多个 LLM 的共识；2) rule_fast 硬编码权重 1.0（最高权重），而 llm_light 仅 0.45、llm_cmd 命中时仅 0.60，权重失衡严重；3) classify() 中 rule_fast 短路返回逻辑一旦误判即直接返回，不给 LLM 纠正机会。日志显示 llm_light(intent=1,conf=0.65) 和 llm_cmd(intent=1,conf=0.7) 均识别为指令，但裁决输出 intent=2(闲聊)conf=0.42，正是这种高权重单票覆盖多票共识的典型表现。


## 反思要求

1. **评估问题归属**：基于上述完整分支记忆（包括 Agent 的决策过程、各能力执行结果、对话历史）和失败日志，严格评估"问题根因是否属于 intent_classifier 模块"。判断时必须同时依据：
   - 失败日志中该模块的直接异常记录
   - 分支内已执行能力（如 log_localization / code_localization）对该模块的具体分析结论
   禁止仅凭直觉或情景描述打分，禁止考虑其他分支模块的表现。

2. **给出置信度**：confidence 必须是 0.0-1.0 的纯数字，表示"**问题根因在该模块**"的可信程度，不是"该模块工作质量评分"。
   - 模块确实有问题 → confidence 越高越确信（0.5~1.0）
   - 模块没有问题 → confidence 必须接近 0.0，表示"问题在该模块"的概率为零
   严禁出现"模块无问题"但 confidence 很高（如 >0.5）的矛盾输出。
   打分规则：
   - 0.90-1.00：日志和分支能力均明确指向该模块存在根因，证据链完整
   - 0.70-0.89：日志或分支能力有较强的指向性，但存在少量可解释的异常
   - 0.40-0.69：证据模棱两可，既有支持也有反驳的线索
   - 0.10-0.39：证据倾向于排除该模块，但无法完全确认
   - 0.00-0.09：日志和分支能力均表明该模块无问题，或问题已被充分排除

   **上下游影响规则（必须严格遵守）**：
   Pipeline 顺序：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。
   - 如果当前模块的异常可以被更上游模块的错误**完全解释**（如上游输出错误数据导致当前模块处理失败），则当前模块的 confidence 必须 ≤ 0.3，因为这是"症状"不是"根因"
   - 只有当当前模块存在**独立缺陷**（与上游无关）时，confidence 才应 ≥ 0.5
   - 严禁因为"当前模块确实报错了"就给高分——必须判断报错是自身缺陷还是上游污染导致的症状

3. **列出关键证据**：evidence 数组中的每条证据必须引用日志原文或能力执行结果中的具体信息（如具体的错误日志行、具体的组件名、具体的根因描述），禁止输出空洞的概括性语句（如"模块表现异常"、"日志显示有问题"）。

## 输出格式

必须且只返回一个纯 JSON 对象，禁止包含 Markdown 代码块标记（```json）、解释文字、思考过程或任何其他内容：

{
  "module": "intent_classifier",
  "confidence": 0.0-1.0,
  "explanation": "为什么问题属于或不属于当前模块",
  "evidence": ["证据1", "证据2"]
}

## Few-Shot 示例

### 示例 1：确认当前模块有问题（独立根因）
输入分支模块：intent_classifier
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"，"⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令"
分支能力结论：log_localization 定位到 failed_component="裁决"

输出：
{
  "module": "intent_classifier",
  "confidence": 0.92,
  "explanation": "日志和分支能力均明确指向该模块的裁决组件：rule_fast(0.8)和llm_cmd(0.7)均识别为指令，但裁决仍输出闲聊intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'",
    "log_localization 结果：failed_component='裁决'"
  ]
}

### 示例 2：排除当前模块（自身无问题）
输入分支模块：command_store
日志证据："[CommandStoreClient] vector_search k=5 score=0.72"，"✅ ES搜索成功: 匹配 cmd_001"
分支能力结论：log_localization 返回 explanation="verified_no_issue"

输出：
{
  "module": "command_store",
  "confidence": 0.02,
  "explanation": "日志显示 vector_search 成功匹配 cmd_001，ES搜索无异常；log_localization 已验证该模块无问题",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'",
    "log_localization 结果：explanation='verified_no_issue'"
  ]
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入分支模块：parameter_extractor
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"（上游已误判），"[ParameterExtractor] regex='往<方向>走' no match in '往前走'"
分支能力结论：log_localization 定位到 regex_match 失败

输出：
{
  "module": "parameter_extractor",
  "confidence": 0.18,
  "explanation": "当前模块的 regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。上游错误导致当前模块接收到了异常输入，regex 匹配失败是症状而非独立根因。根因在上游 intent_classifier 的裁决组件。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游在阶段1已误判为闲聊",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 下游收到异常输入 → regex 匹配失败是症状，不是独立缺陷"
  ]
}

**重要**：示例3 中 confidence 为 0.18（低分），因为当前模块的异常可以被更上游的错误完全解释。严禁因为"当前模块确实出现了报错"就给高分。

## 重要约束

1. module 字段必须严格等于 "intent_classifier"，任何其他值（包括大小写差异）均视为无效输出，会导致解析失败。
2. confidence 必须是 0.0 到 1.0 之间的纯数字，禁止使用字符串形式，禁止超出此范围。
3. 禁止输出 decision、final_module、stop、notify、action 等额外字段。
4. 禁止在 JSON 中输出"停止通知"、"跳过 Finalization"、"通知某某"等流程控制文字。
4. 只反思当前模块 intent_classifier，禁止在 explanation 或 evidence 中与其他模块进行比较或引用其他模块的结论。
6. 禁止在 JSON 前后输出任何 ``` 标记或自然语言解释。""",
          "timestamp": "2026-05-22T15:25:57.964997"
        },
        {
          "step": "reflection_llm",
          "role": "assistant",
          "content": """{
  "module": "intent_classifier",
  "confidence": 0.95,
  "explanation": "日志和分支能力均明确指向 intent_classifier 模块存在独立根因。失败日志中阶段1记录了明显的裁决矛盾：llm_light(intent=1, conf=0.65)和llm_cmd(intent=1, conf=0.7)两个子模块都明确识别为指令，但最终裁决却输出intent=2(闲聊)，conf=0.42，且日志明确标注「裁决失败」。log_localization 将 failed_component 定位为「裁决」，确认裁决组件存在逻辑缺陷。code_localization 进一步深入代码发现：_weighted_vote 使用乘法融合公式 score=Π(1+w*conf) 且 rule_fast 硬编码权重 1.0（最高权重），导致单个高权重投票者可以覆盖多个 LLM 的多数意见共识，这种算法设计允许裁决结果与多数子模块判断相悖。综合日志矛盾、log_localization 定位、code_localization 代码分析三层证据，当前模块的裁决逻辑存在独立缺陷，confidence 0.95 表示根因归属确信度极高。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 最终裁决输出闲聊intent=2",
    "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms' - llm_light明确识别为指令",
    "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms' - llm_cmd明确识别为指令",
    "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决矛盾",
    "log_localization 结果：failed_module='intent_classifier', failed_component='裁决'",
    "code_localization 结果：ensemble.py 的 _weighted_vote 乘法融合公式和 rule_fast 权重1.0导致高权重单票覆盖多票共识，裁决设计存在根本性缺陷"
  ]
}""",
          "timestamp": "2026-05-22T15:25:57.965017"
        }
      ],
      "iteration": [
        {
          "step": "log_localization",
          "evidence": {
            "failed_module": "intent_classifier",
            "failed_component": "裁决",
            "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42 和 ⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令",
            "explanation": "裁决组件存在逻辑缺陷：llm_light(intent=1, conf=0.65)和llm_cmd(intent=1, conf=0.7)两个子模块都明确识别为指令，且总置信度(1.35)高于rule_fast单独识别为闲聊(conf=0.8)。但裁决算法最终输出intent=2(闲聊)，conf=0.42。这种在多数子模块支持指令时仍选择闲聊的决策违背了ensemble投票逻辑，是裁决算法的独立根因问题。",
            "evidence": [
              "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms' - llm_light明确识别为指令",
              "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms' - llm_cmd明确识别为指令",
              "日志行：'[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms' - rule_fast识别为闲聊但仅因chat_pattern",
              "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 最终裁决错误选择闲聊intent=2",
              "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决矛盾"
            ],
            "confidence": 0.85
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.85,
          "timestamp": "2026-05-22T14:52:08.027818"
        },
        {
          "step": "code_localization",
          "evidence": {
            "module": "intent_classifier",
            "confidence": 0.92,
            "explanation": "ensemble.py 的 _weighted_vote 使用乘法融合公式 score=Π(1+w*conf)，且 rule_fast 硬编码权重为 1.0（最高权重）。当日志显示 rule_fast 识别为闲聊(conf=0.8)而 llm_light(intent=1,conf=0.65)和 llm_cmd(intent=1,conf=0.7)都识别为指令时，乘法融合允许单个高权重投票者覆盖多个低权重投票者的多数意见，导致最终裁决错误选择闲聊(intent=2,conf=0.42)。此外 classify() 中 rule_fast 的短路返回逻辑与日志显示的 ensemble 投票行为存在矛盾，表明该模块的裁决设计存在根本性缺陷。",
            "evidence": [
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  366,
                  383,
                  384
                ],
                "code_snippets": [
                  "# 乘法融合：score = Π(1 + w * conf)",
                  "combined = 1.0 + max(0.0, w * c.confidence)",
                  "score_map[c.intent_code] = score_map.get(c.intent_code, 1.0) * combined"
                ]
              },
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  379
                ],
                "code_snippets": [
                  """"rule_fast": 1.0,"""
                ]
              },
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  386,
                  389
                ],
                "code_snippets": [
                  "best_intent = max(score_map.items(), key=lambda kv: kv[1])[0]",
                  "conf = 1.0 - math.exp(-(best_score - 1.0))"
                ]
              },
              {
                "file_path": "config/intent_ensemble.yaml",
                "line_numbers": [
                  5,
                  6,
                  10,
                  11
                ],
                "code_snippets": [
                  "llm: 0.45",
                  "llm_cmd_hit: 0.60",
                  "mem_intent: 0.15",
                  "safety: 0.05"
                ]
              }
            ]
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.92,
          "timestamp": "2026-05-22T15:09:12.884569"
        },
        {
          "step": "code_localization",
          "evidence": {
            "module": "intent_classifier",
            "confidence": 0.92,
            "explanation": "intent_classifier 模块的 ensemble 裁决存在根本性设计缺陷，与日志显示的裁决矛盾高度吻合。核心问题包括：1) _weighted_vote 使用乘法融合公式 score=Π(1+w*conf)，初始值 1.0 不公平地放大了未获投票意图的基数优势，允许单个高权重投票者覆盖多个 LLM 的共识；2) rule_fast 硬编码权重 1.0（最高权重），而 llm_light 仅 0.45、llm_cmd 命中时仅 0.60，权重失衡严重；3) classify() 中 rule_fast 短路返回逻辑一旦误判即直接返回，不给 LLM 纠正机会。日志显示 llm_light(intent=1,conf=0.65) 和 llm_cmd(intent=1,conf=0.7) 均识别为指令，但裁决输出 intent=2(闲聊)conf=0.42，正是这种高权重单票覆盖多票共识的典型表现。",
            "evidence": [
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  362,
                  366,
                  367,
                  379,
                  383,
                  384,
                  386,
                  406,
                  407,
                  409
                ],
                "code_snippets": [
                  "def _weighted_vote(self, candidates: List[IntentCandidate]) -> EnsembleDecision:",
                  "# 乘法融合：score = Π(1 + w * conf)",
                  "score_map: Dict[int, float] = {}",
                  """"rule_fast": 1.0,""",
                  "combined = 1.0 + max(0.0, w * c.confidence)",
                  "score_map[c.intent_code] = score_map.get(c.intent_code, 1.0) * combined",
                  "best_intent = max(score_map.items(), key=lambda kv: kv[1])[0]",
                  "rule_hit = self._rule_fast(text, feature_result)",
                  "if rule_hit:",
                  "    return self._to_intent_result(rule_hit, [rule_hit])"
                ]
              },
              {
                "file_path": "config/intent_ensemble.yaml",
                "line_numbers": [
                  4,
                  5,
                  6
                ],
                "code_snippets": [
                  "weights:",
                  "  llm: 0.45",
                  "  llm_cmd_hit: 0.60"
                ]
              }
            ]
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.92,
          "timestamp": "2026-05-22T15:21:30.595237"
        },
        {
          "step": "reflection",
          "evidence": {
            "confidence": 0.95,
            "explanation": "日志和分支能力均明确指向 intent_classifier 模块存在独立根因。失败日志中阶段1记录了明显的裁决矛盾：llm_light(intent=1, conf=0.65)和llm_cmd(intent=1, conf=0.7)两个子模块都明确识别为指令，但最终裁决却输出intent=2(闲聊)，conf=0.42，且日志明确标注「裁决失败」。log_localization 将 failed_component 定位为「裁决」，确认裁决组件存在逻辑缺陷。code_localization 进一步深入代码发现：_weighted_vote 使用乘法融合公式 score=Π(1+w*conf) 且 rule_fast 硬编码权重 1.0（最高权重），导致单个高权重投票者可以覆盖多个 LLM 的多数意见共识，这种算法设计允许裁决结果与多数子模块判断相悖。综合日志矛盾、log_localization 定位、code_localization 代码分析三层证据，当前模块的裁决逻辑存在独立缺陷，confidence 0.95 表示根因归属确信度极高。",
            "evidence": [
              "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 最终裁决输出闲聊intent=2",
              "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms' - llm_light明确识别为指令",
              "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms' - llm_cmd明确识别为指令",
              "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决矛盾",
              "log_localization 结果：failed_module='intent_classifier', failed_component='裁决'",
              "code_localization 结果：ensemble.py 的 _weighted_vote 乘法融合公式和 rule_fast 权重1.0导致高权重单票覆盖多票共识，裁决设计存在根本性缺陷"
            ],
            "module": "intent_classifier"
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.95,
          "timestamp": "2026-05-22T15:25:57.965024"
        }
      ]
    },
    "parameter_extractor": {
      "context": [
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: parameter_extractor

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
尚未执行任何 capability

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:26:16.185479"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示 parameter_extractor 存在正则匹配失败但回退的行为：regex='往<方向>走' 无法匹配 '往前走'。这种回退掩盖了真实匹配逻辑问题。需要 log_localization 验证模块内部是否有明确的错误堆栈或异常输出，以判断根因是在参数提取的正则定义，还是在上游指令重写阶段传入的参数格式本身不符合预期。"
}""",
          "timestamp": "2026-05-22T15:26:16.185495"
        },
        {
          "step": "log_localization_llm",
          "role": "user",
          "content": """## 任务

你是机器人控制 Pipeline 的日志分析专家。验证可疑模块是否存在问题，定位到具体的组件。

## 待验证模块

模块名称: parameter_extractor
模块置信度: 0.0
怀疑原因: 

模块内部组件: regex_match, capture, default_fallback

**你必须只分析这个模块，不要分析其他模块。**
**通过日志验证该模块是否确实存在问题，并定位到具体的组件。**

## 当前分支记忆

## 上下文

- [llm_decision] assistant: {
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示 parameter_extractor 存在正则匹配失败但回退的行为：regex='往<方向>走' 无法匹配 '往前走'。这种回退掩盖了真实匹配逻辑问题。需要 log_localization 验证模块内部是否有明确的错误堆栈或异常输出，以判断根因是在参数提取的正则定义，还是在上游指令重写阶段传入的参数格式本身不符合预期。"
}


## 输入信息
情景描述: 用户说往前走但机器人没有反应
失败日志:

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 分析要求

1. **只分析上述待验证模块**：不要分析其他模块，即使日志中提到了其他模块
2. **分析该模块的日志表现**：查看该模块在日志中的输出
3. **定位问题组件**：找出该模块中具体哪个组件出了问题
4. **如果该模块没有问题**：返回 explanation = "verified_no_issue"
5. **计算置信度**：根据日志证据的充分性和问题定位的确定性，给出 confidence (0.0~1.0)

## 输出格式

必须返回纯 JSON 对象，禁止包含任何 Markdown 标记、思考过程或解释文字：

{
  "failed_module": "parameter_extractor",
  "failed_component": "该模块内的具体组件名称",
  "error_location": "日志中的具体错误位置",
  "explanation": "根本原因分析，或 'verified_no_issue' 表示该模块没有问题",
  "evidence": ["日志证据1", "日志证据2"],
  "confidence": 确认是当前模块出问题的置信度，0.0~1.0 的数字。
}

## Few-Shot 示例

### 示例 1：模块自身存在问题（独立根因）
输入：待验证模块=intent_classifier，模块组件=["classify", "ensemble", "裁决", "conf_threshold"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
[Ensemble] trace rule_fast: intent=2 conf=0.8
[Ensemble] trace llm_cmd: intent=1 conf=0.7
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
```
输出：
{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件在 rule_fast(0.8)和 llm_cmd(0.7)都识别为指令时仍输出闲聊 intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'"
  ],
  "confidence": 0.9
}

### 示例 2：模块没有问题
输入：待验证模块=command_store，模块组件=["vector_search", "text_search", "merge_results"]
日志：
```
[CommandStoreClient] vector_search k=5 score=0.72
[CommandStoreClient] matched_command: {"id": "cmd_001"}
✅ ES搜索成功: 匹配 cmd_001
```
输出：
{
  "failed_module": "command_store",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'"
  ],
  "confidence": 0.0
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入：待验证模块=parameter_extractor，模块组件=["regex_match", "capture", "default_fallback"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"
```
输出：
{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex="往<方向>走" no match in "往前走"",
  "explanation": "regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。当前模块接收到的输入已被上游错误污染（'往前走'被误判为闲聊意图，导致 regex 模式不匹配）。这是上游错误导致的症状，不是 parameter_extractor 的独立根因。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游意图分类已误判",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 当前模块收到异常输入 → regex 匹配失败是症状而非独立缺陷"
  ],
  "confidence": 0.2
}

**重要**：示例3 中 confidence 为 0.2（低分），因为虽然当前模块确实出现了异常，但其异常可以被更上游的错误完全解释。根因是上游的 intent_classifier，不是当前模块。

## confidence 语义（必须严格遵守）

confidence 表示"**日志证据支持'问题根因在该模块'的置信度**"，不是"该模块工作质量评分"。

- 模块**确实有问题** → confidence 越高表示越确信（0.5~1.0）
- 模块**没有问题**（explanation = "verified_no_issue"）→ confidence **必须为 0.0**，表示"问题在该模块"的概率为零

严禁出现 explanation="verified_no_issue" 但 confidence > 0 的矛盾输出。

## 重要约束

1. **只输出 JSON**：禁止输出 ```json、```、解释文字或思考过程
2. **failed_module**：必须返回 "parameter_extractor"
3. **explanation**：如果该模块没有问题，必须返回 "verified_no_issue"，**此时 confidence 必须为 0.0**
4. **confidence**：必须返回 0.0~1.0 的数字，语义是"问题根因在该模块"的置信度
5. **evidence**：必须列出支撑 explanation 的具体日志证据，不能为空
5. **只分析待验证模块**：不要分析其他模块
6. **所有字段必须完整**：输出必须包含全部 5 个字段，缺一不可""",
          "timestamp": "2026-05-22T15:26:20.952667"
        },
        {
          "step": "log_localization_llm",
          "role": "assistant",
          "content": """{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"",
  "explanation": "regex_match 组件的正则表达式定义存在问题：模式 '往<方向>走' 无法匹配有效输入 '往前走'。输入字符串 '往前走' 中包含 '前' 这个方向参数，应当能被正确捕获，但 regex_match 却返回 no match。这表明正则定义本身有缺陷（可能是捕获组边界定义错误或模式转义问题），而不是上游传入的参数格式问题。即使指令重写阶段输出的 '往前走' 是正确的，regex_match 组件仍无法识别。",
  "evidence": [
    "日志行：'[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"' - regex_match 匹配失败",
    "日志行：'⚠️  参数提取失败: 缺少\"方向\"参数，回退到默认\"前\"' - 回退机制被触发",
    "日志行：'[InstructionRewriter] rewrite: \"往前走\" → \"往前走\"' - 上游指令重写输出的内容正确，无格式异常",
    "因果分析：指令重写输出正确 → 输入到 parameter_extractor 的字符串本身无问题 → regex_match 匹配失败是 regex 定义缺陷而非输入问题"
  ],
  "confidence": 0.75
}""",
          "timestamp": "2026-05-22T15:26:20.952686"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: parameter_extractor

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:28:55.638903"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{"capability": null, "end_signal": true, "reason": "参数提取正则不匹配是表面现象但非根因：1) regex虽未匹配但协议仍成功生成并通过MQTT发出，2) 设备无响应的最终失败点在emqx_client/设备层，与parameter_extractor无直接因果，3) 已执行过log_localization。结合置信度≤0.3的排除标准，当前证据已足够支持排除当前模块，应结束分支进入Reflection进一步定位真正根因"}""",
          "timestamp": "2026-05-22T15:28:55.638933"
        },
        {
          "step": "reflection_llm",
          "role": "user",
          "content": """## 任务

你是当前诊断分支的反思器。你的唯一任务是：基于分支内已执行的能力结果和失败日志，评估"问题根因是否属于当前分支的模块 parameter_extractor"。

你禁止执行任何其他判断，包括但不限于：
- 禁止决定是否发送通知
- 禁止决定整个诊断流程是否停止
- 禁止将其他模块指定为问题模块
- 禁止输出任何与 JSON 格式无关的思考过程或解释文字

## 当前分支模块

parameter_extractor

## 情景描述

用户说往前走但机器人没有反应

## 失败日志


# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 当前分支完整记忆（包含决策对话和能力执行结果）

## 上下文

- [llm_decision] assistant: {
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示 parameter_extractor 存在正则匹配失败但回退的行为：regex='往<方向>走' 无法匹配 '往前走'。这种回退掩盖了真实匹配逻辑问题。需要 log_localization 验证模块内部是否有明确的错误堆栈或异常输出，以判断根因是在参数提取的正则定义，还是在上游指令重写阶段传入的参数格式本身不符合预期。"
}
- [log_localization_llm] assistant: {
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"",
  "explanation": "regex_match 组件的正则表达式定义存在问题：模式 '往<方向>走' 无法匹配有效输入 '往前走'。输入字符串 '往前走' 中包含 '前' 这个方向参数，应当能被正确捕获，但 regex_match 却返回 no match。这表明正则定义本身有缺陷（可能是捕获组边界定义错误或模式转义问题），而不是上游传入的参数格式问题。即使指令重写阶段输出的 '往前走' 是正确的，regex_match 组件仍无法识别。",
  "evidence": [
    "日志行：'[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"' - regex_match 匹配失败",
    "日志行：'⚠️  参数提取失败: 缺少\"方向\"参数，回退到默认\"前\"' - 回退机制被触发",
    "日志行：'[InstructionRewriter] rewrite: \"往前走\" → \"往前走\"' - 上游指令重写输出的内容正确，无格式异常",
    "因果分析：指令重写输出正确 → 输入到 parameter_extractor 的字符串本身无问题 → regex_match 匹配失败是 regex 定义缺陷而非输入问题"
  ],
  "confidence": 0.75
}
- [llm_decision] assistant: {"capability": null, "end_signal": true, "reason": "参数提取正则不匹配是表面现象但非根因：1) regex虽未匹配但协议仍成功生成并通过MQTT发出，2) 设备无响应的最终失败点在emqx_client/设备层，与parameter_extractor无直接因果，3) 已执行过log_localization。结合置信度≤0.3的排除标准，当前证据已足够支持排除当前模块，应结束分支进入Reflection进一步定位真正根因"}

## 能力使用

- ✅ log_localization (confidence=0.75)
  failed_module: parameter_extractor
  explanation: regex_match 组件的正则表达式定义存在问题：模式 '往<方向>走' 无法匹配有效输入 '往前走'。输入字符串 '往前走' 中包含 '前' 这个方向参数，应当能被正确捕获，但 regex_match 却返回 no match。这表明正则定义本身有缺陷（可能是捕获组边界定义错误或模式转义问题），而不是上游传入的参数格式问题。即使指令重写阶段输出的 '往前走' 是正确的，regex_match 组件仍无法识别。


## 反思要求

1. **评估问题归属**：基于上述完整分支记忆（包括 Agent 的决策过程、各能力执行结果、对话历史）和失败日志，严格评估"问题根因是否属于 parameter_extractor 模块"。判断时必须同时依据：
   - 失败日志中该模块的直接异常记录
   - 分支内已执行能力（如 log_localization / code_localization）对该模块的具体分析结论
   禁止仅凭直觉或情景描述打分，禁止考虑其他分支模块的表现。

2. **给出置信度**：confidence 必须是 0.0-1.0 的纯数字，表示"**问题根因在该模块**"的可信程度，不是"该模块工作质量评分"。
   - 模块确实有问题 → confidence 越高越确信（0.5~1.0）
   - 模块没有问题 → confidence 必须接近 0.0，表示"问题在该模块"的概率为零
   严禁出现"模块无问题"但 confidence 很高（如 >0.5）的矛盾输出。
   打分规则：
   - 0.90-1.00：日志和分支能力均明确指向该模块存在根因，证据链完整
   - 0.70-0.89：日志或分支能力有较强的指向性，但存在少量可解释的异常
   - 0.40-0.69：证据模棱两可，既有支持也有反驳的线索
   - 0.10-0.39：证据倾向于排除该模块，但无法完全确认
   - 0.00-0.09：日志和分支能力均表明该模块无问题，或问题已被充分排除

   **上下游影响规则（必须严格遵守）**：
   Pipeline 顺序：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。
   - 如果当前模块的异常可以被更上游模块的错误**完全解释**（如上游输出错误数据导致当前模块处理失败），则当前模块的 confidence 必须 ≤ 0.3，因为这是"症状"不是"根因"
   - 只有当当前模块存在**独立缺陷**（与上游无关）时，confidence 才应 ≥ 0.5
   - 严禁因为"当前模块确实报错了"就给高分——必须判断报错是自身缺陷还是上游污染导致的症状

3. **列出关键证据**：evidence 数组中的每条证据必须引用日志原文或能力执行结果中的具体信息（如具体的错误日志行、具体的组件名、具体的根因描述），禁止输出空洞的概括性语句（如"模块表现异常"、"日志显示有问题"）。

## 输出格式

必须且只返回一个纯 JSON 对象，禁止包含 Markdown 代码块标记（```json）、解释文字、思考过程或任何其他内容：

{
  "module": "parameter_extractor",
  "confidence": 0.0-1.0,
  "explanation": "为什么问题属于或不属于当前模块",
  "evidence": ["证据1", "证据2"]
}

## Few-Shot 示例

### 示例 1：确认当前模块有问题（独立根因）
输入分支模块：intent_classifier
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"，"⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令"
分支能力结论：log_localization 定位到 failed_component="裁决"

输出：
{
  "module": "intent_classifier",
  "confidence": 0.92,
  "explanation": "日志和分支能力均明确指向该模块的裁决组件：rule_fast(0.8)和llm_cmd(0.7)均识别为指令，但裁决仍输出闲聊intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'",
    "log_localization 结果：failed_component='裁决'"
  ]
}

### 示例 2：排除当前模块（自身无问题）
输入分支模块：command_store
日志证据："[CommandStoreClient] vector_search k=5 score=0.72"，"✅ ES搜索成功: 匹配 cmd_001"
分支能力结论：log_localization 返回 explanation="verified_no_issue"

输出：
{
  "module": "command_store",
  "confidence": 0.02,
  "explanation": "日志显示 vector_search 成功匹配 cmd_001，ES搜索无异常；log_localization 已验证该模块无问题",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'",
    "log_localization 结果：explanation='verified_no_issue'"
  ]
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入分支模块：parameter_extractor
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"（上游已误判），"[ParameterExtractor] regex='往<方向>走' no match in '往前走'"
分支能力结论：log_localization 定位到 regex_match 失败

输出：
{
  "module": "parameter_extractor",
  "confidence": 0.18,
  "explanation": "当前模块的 regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。上游错误导致当前模块接收到了异常输入，regex 匹配失败是症状而非独立根因。根因在上游 intent_classifier 的裁决组件。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游在阶段1已误判为闲聊",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 下游收到异常输入 → regex 匹配失败是症状，不是独立缺陷"
  ]
}

**重要**：示例3 中 confidence 为 0.18（低分），因为当前模块的异常可以被更上游的错误完全解释。严禁因为"当前模块确实出现了报错"就给高分。

## 重要约束

1. module 字段必须严格等于 "parameter_extractor"，任何其他值（包括大小写差异）均视为无效输出，会导致解析失败。
2. confidence 必须是 0.0 到 1.0 之间的纯数字，禁止使用字符串形式，禁止超出此范围。
3. 禁止输出 decision、final_module、stop、notify、action 等额外字段。
4. 禁止在 JSON 中输出"停止通知"、"跳过 Finalization"、"通知某某"等流程控制文字。
4. 只反思当前模块 parameter_extractor，禁止在 explanation 或 evidence 中与其他模块进行比较或引用其他模块的结论。
6. 禁止在 JSON 前后输出任何 ``` 标记或自然语言解释。""",
          "timestamp": "2026-05-22T15:29:20.128479"
        },
        {
          "step": "reflection_llm",
          "role": "assistant",
          "content": """{
  "module": "parameter_extractor",
  "confidence": 0.12,
  "explanation": "parameter_extractor 的 regex_match 组件确实返回了匹配失败日志 '[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"'，但该异常是表面症状而非独立根因。原因如下：1) 匹配失败触发了回退机制，系统自动回退到默认方向\"前\"；2) 协议生成阶段成功完成 '[ProtocolBuilder] template=\"move_forward\" found'，说明参数缺失被妥善处理；3) 最终失败点在 MQTT 发送后的设备无响应阶段 '[EMQXClient] ❌ 重试耗尽，设备无响应'，与 parameter_extractor 无直接因果关系。因此 regex_match 的匹配失败不构成独立根因，而是可以被系统回退机制吸收的异常。",
  "evidence": [
    "日志行：'[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"' - 正则匹配确实失败",
    "日志行：'⚠️ 参数提取失败: 缺少\"方向\"参数，回退到默认\"前\"' - 回退机制成功接管",
    "日志行：'[ProtocolBuilder] template=\"move_forward\" found' - 协议仍成功生成",
    "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - 最终失败点在 MQTT/设备层，非参数提取导致",
    "log_localization 结论：regex_match 失败被回退机制处理，指令重写验证上游输入正确"
  ]
}""",
          "timestamp": "2026-05-22T15:29:20.128498"
        }
      ],
      "iteration": [
        {
          "step": "log_localization",
          "evidence": {
            "failed_module": "parameter_extractor",
            "failed_component": "regex_match",
            "error_location": """[ParameterExtractor] regex="往<方向>走" no match in "往前走"""",
            "explanation": "regex_match 组件的正则表达式定义存在问题：模式 '往<方向>走' 无法匹配有效输入 '往前走'。输入字符串 '往前走' 中包含 '前' 这个方向参数，应当能被正确捕获，但 regex_match 却返回 no match。这表明正则定义本身有缺陷（可能是捕获组边界定义错误或模式转义问题），而不是上游传入的参数格式问题。即使指令重写阶段输出的 '往前走' 是正确的，regex_match 组件仍无法识别。",
            "evidence": [
              """日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - regex_match 匹配失败""",
              """日志行：'⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"' - 回退机制被触发""",
              """日志行：'[InstructionRewriter] rewrite: "往前走" → "往前走"' - 上游指令重写输出的内容正确，无格式异常""",
              "因果分析：指令重写输出正确 → 输入到 parameter_extractor 的字符串本身无问题 → regex_match 匹配失败是 regex 定义缺陷而非输入问题"
            ],
            "confidence": 0.75
          },
          "success": true,
          "target_module": "parameter_extractor",
          "confidence": 0.75,
          "timestamp": "2026-05-22T15:26:20.952694"
        },
        {
          "step": "reflection",
          "evidence": {
            "confidence": 0.12,
            "explanation": """parameter_extractor 的 regex_match 组件确实返回了匹配失败日志 '[ParameterExtractor] regex="往<方向>走" no match in "往前走"'，但该异常是表面症状而非独立根因。原因如下：1) 匹配失败触发了回退机制，系统自动回退到默认方向"前"；2) 协议生成阶段成功完成 '[ProtocolBuilder] template="move_forward" found'，说明参数缺失被妥善处理；3) 最终失败点在 MQTT 发送后的设备无响应阶段 '[EMQXClient] ❌ 重试耗尽，设备无响应'，与 parameter_extractor 无直接因果关系。因此 regex_match 的匹配失败不构成独立根因，而是可以被系统回退机制吸收的异常。""",
            "evidence": [
              """日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 正则匹配确实失败""",
              """日志行：'⚠️ 参数提取失败: 缺少"方向"参数，回退到默认"前"' - 回退机制成功接管""",
              """日志行：'[ProtocolBuilder] template="move_forward" found' - 协议仍成功生成""",
              "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - 最终失败点在 MQTT/设备层，非参数提取导致",
              "log_localization 结论：regex_match 失败被回退机制处理，指令重写验证上游输入正确"
            ],
            "module": "parameter_extractor"
          },
          "success": true,
          "target_module": "parameter_extractor",
          "confidence": 0.12,
          "timestamp": "2026-05-22T15:29:20.128504"
        }
      ]
    },
    "protocol_builder": {
      "context": [
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: protocol_builder

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
尚未执行任何 capability

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:29:36.701675"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示协议生成成功（template=move_forward），但设备最终无响应。需要验证 protocol_builder 生成的协议内容是否符合预期、MQTT payload 是否正确"
}
```""",
          "timestamp": "2026-05-22T15:29:36.701691"
        },
        {
          "step": "log_localization_llm",
          "role": "user",
          "content": """## 任务

你是机器人控制 Pipeline 的日志分析专家。验证可疑模块是否存在问题，定位到具体的组件。

## 待验证模块

模块名称: protocol_builder
模块置信度: 0.0
怀疑原因: 

模块内部组件: template_match, param_substitute

**你必须只分析这个模块，不要分析其他模块。**
**通过日志验证该模块是否确实存在问题，并定位到具体的组件。**

## 当前分支记忆

## 上下文

- [llm_decision] assistant: ```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示协议生成成功（template=move_forward），但设备最终无响应。需要验证 protocol_builder 生成的协议内容是否符合预期、MQTT payload 是否正确"
}
```


## 输入信息
情景描述: 用户说往前走但机器人没有反应
失败日志:

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 分析要求

1. **只分析上述待验证模块**：不要分析其他模块，即使日志中提到了其他模块
2. **分析该模块的日志表现**：查看该模块在日志中的输出
3. **定位问题组件**：找出该模块中具体哪个组件出了问题
4. **如果该模块没有问题**：返回 explanation = "verified_no_issue"
5. **计算置信度**：根据日志证据的充分性和问题定位的确定性，给出 confidence (0.0~1.0)

## 输出格式

必须返回纯 JSON 对象，禁止包含任何 Markdown 标记、思考过程或解释文字：

{
  "failed_module": "protocol_builder",
  "failed_component": "该模块内的具体组件名称",
  "error_location": "日志中的具体错误位置",
  "explanation": "根本原因分析，或 'verified_no_issue' 表示该模块没有问题",
  "evidence": ["日志证据1", "日志证据2"],
  "confidence": 确认是当前模块出问题的置信度，0.0~1.0 的数字。
}

## Few-Shot 示例

### 示例 1：模块自身存在问题（独立根因）
输入：待验证模块=intent_classifier，模块组件=["classify", "ensemble", "裁决", "conf_threshold"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
[Ensemble] trace rule_fast: intent=2 conf=0.8
[Ensemble] trace llm_cmd: intent=1 conf=0.7
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
```
输出：
{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件在 rule_fast(0.8)和 llm_cmd(0.7)都识别为指令时仍输出闲聊 intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'"
  ],
  "confidence": 0.9
}

### 示例 2：模块没有问题
输入：待验证模块=command_store，模块组件=["vector_search", "text_search", "merge_results"]
日志：
```
[CommandStoreClient] vector_search k=5 score=0.72
[CommandStoreClient] matched_command: {"id": "cmd_001"}
✅ ES搜索成功: 匹配 cmd_001
```
输出：
{
  "failed_module": "command_store",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'"
  ],
  "confidence": 0.0
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入：待验证模块=parameter_extractor，模块组件=["regex_match", "capture", "default_fallback"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"
```
输出：
{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex="往<方向>走" no match in "往前走"",
  "explanation": "regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。当前模块接收到的输入已被上游错误污染（'往前走'被误判为闲聊意图，导致 regex 模式不匹配）。这是上游错误导致的症状，不是 parameter_extractor 的独立根因。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游意图分类已误判",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 当前模块收到异常输入 → regex 匹配失败是症状而非独立缺陷"
  ],
  "confidence": 0.2
}

**重要**：示例3 中 confidence 为 0.2（低分），因为虽然当前模块确实出现了异常，但其异常可以被更上游的错误完全解释。根因是上游的 intent_classifier，不是当前模块。

## confidence 语义（必须严格遵守）

confidence 表示"**日志证据支持'问题根因在该模块'的置信度**"，不是"该模块工作质量评分"。

- 模块**确实有问题** → confidence 越高表示越确信（0.5~1.0）
- 模块**没有问题**（explanation = "verified_no_issue"）→ confidence **必须为 0.0**，表示"问题在该模块"的概率为零

严禁出现 explanation="verified_no_issue" 但 confidence > 0 的矛盾输出。

## 重要约束

1. **只输出 JSON**：禁止输出 ```json、```、解释文字或思考过程
2. **failed_module**：必须返回 "protocol_builder"
3. **explanation**：如果该模块没有问题，必须返回 "verified_no_issue"，**此时 confidence 必须为 0.0**
4. **confidence**：必须返回 0.0~1.0 的数字，语义是"问题根因在该模块"的置信度
5. **evidence**：必须列出支撑 explanation 的具体日志证据，不能为空
5. **只分析待验证模块**：不要分析其他模块
6. **所有字段必须完整**：输出必须包含全部 5 个字段，缺一不可""",
          "timestamp": "2026-05-22T15:29:54.914719"
        },
        {
          "step": "log_localization_llm",
          "role": "assistant",
          "content": """{
  "failed_module": "protocol_builder",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[ProtocolBuilder] template=\"move_forward\" found' - 模板匹配成功找到正确模板",
    "日志行：'✅ 协议生成成功' - 协议生成完成，无异常"
  ],
  "confidence": 0.0
}""",
          "timestamp": "2026-05-22T15:29:54.914739"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: protocol_builder

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:30:10.434085"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{
  "capability": null,
  "end_signal": true,
  "reason": "protocol_builder 模块日志显示 '✅ 协议生成成功'，没有报错。MQTT 发送失败（设备无响应）是下游 EMQX/设备层的问题，与当前模块生成协议的行为无关。已执行过 log_localization，当前证据明确支持排除 protocol_builder 为根因模块，应结束分支进入 Reflection 确认上游意图分类矛盾和下游 EMQX 连接问题"
}""",
          "timestamp": "2026-05-22T15:30:10.434106"
        },
        {
          "step": "reflection_llm",
          "role": "user",
          "content": """## 任务

你是当前诊断分支的反思器。你的唯一任务是：基于分支内已执行的能力结果和失败日志，评估"问题根因是否属于当前分支的模块 protocol_builder"。

你禁止执行任何其他判断，包括但不限于：
- 禁止决定是否发送通知
- 禁止决定整个诊断流程是否停止
- 禁止将其他模块指定为问题模块
- 禁止输出任何与 JSON 格式无关的思考过程或解释文字

## 当前分支模块

protocol_builder

## 情景描述

用户说往前走但机器人没有反应

## 失败日志


# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 当前分支完整记忆（包含决策对话和能力执行结果）

## 上下文

- [llm_decision] assistant: ```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示协议生成成功（template=move_forward），但设备最终无响应。需要验证 protocol_builder 生成的协议内容是否符合预期、MQTT payload 是否正确"
}
```
- [log_localization_llm] assistant: {
  "failed_module": "protocol_builder",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[ProtocolBuilder] template=\"move_forward\" found' - 模板匹配成功找到正确模板",
    "日志行：'✅ 协议生成成功' - 协议生成完成，无异常"
  ],
  "confidence": 0.0
}
- [llm_decision] assistant: {
  "capability": null,
  "end_signal": true,
  "reason": "protocol_builder 模块日志显示 '✅ 协议生成成功'，没有报错。MQTT 发送失败（设备无响应）是下游 EMQX/设备层的问题，与当前模块生成协议的行为无关。已执行过 log_localization，当前证据明确支持排除 protocol_builder 为根因模块，应结束分支进入 Reflection 确认上游意图分类矛盾和下游 EMQX 连接问题"
}

## 能力使用

- ✅ log_localization (confidence=0.00)
  failed_module: protocol_builder
  explanation: verified_no_issue


## 反思要求

1. **评估问题归属**：基于上述完整分支记忆（包括 Agent 的决策过程、各能力执行结果、对话历史）和失败日志，严格评估"问题根因是否属于 protocol_builder 模块"。判断时必须同时依据：
   - 失败日志中该模块的直接异常记录
   - 分支内已执行能力（如 log_localization / code_localization）对该模块的具体分析结论
   禁止仅凭直觉或情景描述打分，禁止考虑其他分支模块的表现。

2. **给出置信度**：confidence 必须是 0.0-1.0 的纯数字，表示"**问题根因在该模块**"的可信程度，不是"该模块工作质量评分"。
   - 模块确实有问题 → confidence 越高越确信（0.5~1.0）
   - 模块没有问题 → confidence 必须接近 0.0，表示"问题在该模块"的概率为零
   严禁出现"模块无问题"但 confidence 很高（如 >0.5）的矛盾输出。
   打分规则：
   - 0.90-1.00：日志和分支能力均明确指向该模块存在根因，证据链完整
   - 0.70-0.89：日志或分支能力有较强的指向性，但存在少量可解释的异常
   - 0.40-0.69：证据模棱两可，既有支持也有反驳的线索
   - 0.10-0.39：证据倾向于排除该模块，但无法完全确认
   - 0.00-0.09：日志和分支能力均表明该模块无问题，或问题已被充分排除

   **上下游影响规则（必须严格遵守）**：
   Pipeline 顺序：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。
   - 如果当前模块的异常可以被更上游模块的错误**完全解释**（如上游输出错误数据导致当前模块处理失败），则当前模块的 confidence 必须 ≤ 0.3，因为这是"症状"不是"根因"
   - 只有当当前模块存在**独立缺陷**（与上游无关）时，confidence 才应 ≥ 0.5
   - 严禁因为"当前模块确实报错了"就给高分——必须判断报错是自身缺陷还是上游污染导致的症状

3. **列出关键证据**：evidence 数组中的每条证据必须引用日志原文或能力执行结果中的具体信息（如具体的错误日志行、具体的组件名、具体的根因描述），禁止输出空洞的概括性语句（如"模块表现异常"、"日志显示有问题"）。

## 输出格式

必须且只返回一个纯 JSON 对象，禁止包含 Markdown 代码块标记（```json）、解释文字、思考过程或任何其他内容：

{
  "module": "protocol_builder",
  "confidence": 0.0-1.0,
  "explanation": "为什么问题属于或不属于当前模块",
  "evidence": ["证据1", "证据2"]
}

## Few-Shot 示例

### 示例 1：确认当前模块有问题（独立根因）
输入分支模块：intent_classifier
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"，"⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令"
分支能力结论：log_localization 定位到 failed_component="裁决"

输出：
{
  "module": "intent_classifier",
  "confidence": 0.92,
  "explanation": "日志和分支能力均明确指向该模块的裁决组件：rule_fast(0.8)和llm_cmd(0.7)均识别为指令，但裁决仍输出闲聊intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'",
    "log_localization 结果：failed_component='裁决'"
  ]
}

### 示例 2：排除当前模块（自身无问题）
输入分支模块：command_store
日志证据："[CommandStoreClient] vector_search k=5 score=0.72"，"✅ ES搜索成功: 匹配 cmd_001"
分支能力结论：log_localization 返回 explanation="verified_no_issue"

输出：
{
  "module": "command_store",
  "confidence": 0.02,
  "explanation": "日志显示 vector_search 成功匹配 cmd_001，ES搜索无异常；log_localization 已验证该模块无问题",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'",
    "log_localization 结果：explanation='verified_no_issue'"
  ]
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入分支模块：parameter_extractor
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"（上游已误判），"[ParameterExtractor] regex='往<方向>走' no match in '往前走'"
分支能力结论：log_localization 定位到 regex_match 失败

输出：
{
  "module": "parameter_extractor",
  "confidence": 0.18,
  "explanation": "当前模块的 regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。上游错误导致当前模块接收到了异常输入，regex 匹配失败是症状而非独立根因。根因在上游 intent_classifier 的裁决组件。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游在阶段1已误判为闲聊",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 下游收到异常输入 → regex 匹配失败是症状，不是独立缺陷"
  ]
}

**重要**：示例3 中 confidence 为 0.18（低分），因为当前模块的异常可以被更上游的错误完全解释。严禁因为"当前模块确实出现了报错"就给高分。

## 重要约束

1. module 字段必须严格等于 "protocol_builder"，任何其他值（包括大小写差异）均视为无效输出，会导致解析失败。
2. confidence 必须是 0.0 到 1.0 之间的纯数字，禁止使用字符串形式，禁止超出此范围。
3. 禁止输出 decision、final_module、stop、notify、action 等额外字段。
4. 禁止在 JSON 中输出"停止通知"、"跳过 Finalization"、"通知某某"等流程控制文字。
4. 只反思当前模块 protocol_builder，禁止在 explanation 或 evidence 中与其他模块进行比较或引用其他模块的结论。
6. 禁止在 JSON 前后输出任何 ``` 标记或自然语言解释。""",
          "timestamp": "2026-05-22T15:30:52.258451"
        },
        {
          "step": "reflection_llm",
          "role": "assistant",
          "content": """{
  "module": "protocol_builder",
  "confidence": 0.0,
  "explanation": "日志和分支能力均表明 protocol_builder 模块无问题。日志行显示模板匹配成功找到 'move_forward' 模板，且协议生成完成无任何异常。log_localization 能力已验证该模块无问题，返回 explanation='verified_no_issue' 和 confidence=0.0。MQTT 发送后设备无响应的根因位于下游 EMQX/设备层，属于 EMQXClient 模块的问题，与 protocol_builder 生成协议的行为无关。",
  "evidence": [
    "日志行：'[ProtocolBuilder] template=\"move_forward\" found' - 模板匹配成功找到正确模板",
    "日志行：'✅ 协议生成成功' - 协议生成完成，无异常抛出",
    "log_localization 能力返回：explanation='verified_no_issue'，confidence=0.0，明确验证该模块无问题",
    "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - MQTT 发送失败发生在下游 EMQX 层，与协议生成无关"
  ]
}""",
          "timestamp": "2026-05-22T15:30:52.258472"
        }
      ],
      "iteration": [
        {
          "step": "log_localization",
          "evidence": {
            "failed_module": "protocol_builder",
            "failed_component": "N/A",
            "error_location": "N/A",
            "explanation": "verified_no_issue",
            "evidence": [
              """日志行：'[ProtocolBuilder] template="move_forward" found' - 模板匹配成功找到正确模板""",
              "日志行：'✅ 协议生成成功' - 协议生成完成，无异常"
            ],
            "confidence": 0.0
          },
          "success": true,
          "target_module": "protocol_builder",
          "confidence": 0.0,
          "timestamp": "2026-05-22T15:29:54.914750"
        },
        {
          "step": "reflection",
          "evidence": {
            "confidence": 0.0,
            "explanation": "日志和分支能力均表明 protocol_builder 模块无问题。日志行显示模板匹配成功找到 'move_forward' 模板，且协议生成完成无任何异常。log_localization 能力已验证该模块无问题，返回 explanation='verified_no_issue' 和 confidence=0.0。MQTT 发送后设备无响应的根因位于下游 EMQX/设备层，属于 EMQXClient 模块的问题，与 protocol_builder 生成协议的行为无关。",
            "evidence": [
              """日志行：'[ProtocolBuilder] template="move_forward" found' - 模板匹配成功找到正确模板""",
              "日志行：'✅ 协议生成成功' - 协议生成完成，无异常抛出",
              "log_localization 能力返回：explanation='verified_no_issue'，confidence=0.0，明确验证该模块无问题",
              "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - MQTT 发送失败发生在下游 EMQX 层，与协议生成无关"
            ],
            "module": "protocol_builder"
          },
          "success": true,
          "target_module": "protocol_builder",
          "confidence": 0.0,
          "timestamp": "2026-05-22T15:30:52.258479"
        }
      ]
    },
    "emqx_client": {
      "context": [
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: emqx_client

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
尚未执行任何 capability

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:31:10.384965"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{"capability": "log_localization", "end_signal": false, "reason": "日志显示 emqx_client 正常发布到 MQTT topic 并等待回调，但设备无响应导致超时。需要通过 log_localization 验证该模块是否存在独立代码异常，或仅是下游设备无响应的症状"}""",
          "timestamp": "2026-05-22T15:31:10.384983"
        },
        {
          "step": "log_localization_llm",
          "role": "user",
          "content": """## 任务

你是机器人控制 Pipeline 的日志分析专家。验证可疑模块是否存在问题，定位到具体的组件。

## 待验证模块

模块名称: emqx_client
模块置信度: 0.0
怀疑原因: 

模块内部组件: publish, subscribe, callback, timeout_handler, retry_logic

**你必须只分析这个模块，不要分析其他模块。**
**通过日志验证该模块是否确实存在问题，并定位到具体的组件。**

## 当前分支记忆

## 上下文

- [llm_decision] assistant: {"capability": "log_localization", "end_signal": false, "reason": "日志显示 emqx_client 正常发布到 MQTT topic 并等待回调，但设备无响应导致超时。需要通过 log_localization 验证该模块是否存在独立代码异常，或仅是下游设备无响应的症状"}


## 输入信息
情景描述: 用户说往前走但机器人没有反应
失败日志:

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 分析要求

1. **只分析上述待验证模块**：不要分析其他模块，即使日志中提到了其他模块
2. **分析该模块的日志表现**：查看该模块在日志中的输出
3. **定位问题组件**：找出该模块中具体哪个组件出了问题
4. **如果该模块没有问题**：返回 explanation = "verified_no_issue"
5. **计算置信度**：根据日志证据的充分性和问题定位的确定性，给出 confidence (0.0~1.0)

## 输出格式

必须返回纯 JSON 对象，禁止包含任何 Markdown 标记、思考过程或解释文字：

{
  "failed_module": "emqx_client",
  "failed_component": "该模块内的具体组件名称",
  "error_location": "日志中的具体错误位置",
  "explanation": "根本原因分析，或 'verified_no_issue' 表示该模块没有问题",
  "evidence": ["日志证据1", "日志证据2"],
  "confidence": 确认是当前模块出问题的置信度，0.0~1.0 的数字。
}

## Few-Shot 示例

### 示例 1：模块自身存在问题（独立根因）
输入：待验证模块=intent_classifier，模块组件=["classify", "ensemble", "裁决", "conf_threshold"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
[Ensemble] trace rule_fast: intent=2 conf=0.8
[Ensemble] trace llm_cmd: intent=1 conf=0.7
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
```
输出：
{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件在 rule_fast(0.8)和 llm_cmd(0.7)都识别为指令时仍输出闲聊 intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'"
  ],
  "confidence": 0.9
}

### 示例 2：模块没有问题
输入：待验证模块=command_store，模块组件=["vector_search", "text_search", "merge_results"]
日志：
```
[CommandStoreClient] vector_search k=5 score=0.72
[CommandStoreClient] matched_command: {"id": "cmd_001"}
✅ ES搜索成功: 匹配 cmd_001
```
输出：
{
  "failed_module": "command_store",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'"
  ],
  "confidence": 0.0
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入：待验证模块=parameter_extractor，模块组件=["regex_match", "capture", "default_fallback"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"
```
输出：
{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex="往<方向>走" no match in "往前走"",
  "explanation": "regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。当前模块接收到的输入已被上游错误污染（'往前走'被误判为闲聊意图，导致 regex 模式不匹配）。这是上游错误导致的症状，不是 parameter_extractor 的独立根因。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游意图分类已误判",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 当前模块收到异常输入 → regex 匹配失败是症状而非独立缺陷"
  ],
  "confidence": 0.2
}

**重要**：示例3 中 confidence 为 0.2（低分），因为虽然当前模块确实出现了异常，但其异常可以被更上游的错误完全解释。根因是上游的 intent_classifier，不是当前模块。

## confidence 语义（必须严格遵守）

confidence 表示"**日志证据支持'问题根因在该模块'的置信度**"，不是"该模块工作质量评分"。

- 模块**确实有问题** → confidence 越高表示越确信（0.5~1.0）
- 模块**没有问题**（explanation = "verified_no_issue"）→ confidence **必须为 0.0**，表示"问题在该模块"的概率为零

严禁出现 explanation="verified_no_issue" 但 confidence > 0 的矛盾输出。

## 重要约束

1. **只输出 JSON**：禁止输出 ```json、```、解释文字或思考过程
2. **failed_module**：必须返回 "emqx_client"
3. **explanation**：如果该模块没有问题，必须返回 "verified_no_issue"，**此时 confidence 必须为 0.0**
4. **confidence**：必须返回 0.0~1.0 的数字，语义是"问题根因在该模块"的置信度
5. **evidence**：必须列出支撑 explanation 的具体日志证据，不能为空
5. **只分析待验证模块**：不要分析其他模块
6. **所有字段必须完整**：输出必须包含全部 5 个字段，缺一不可""",
          "timestamp": "2026-05-22T15:31:30.514663"
        },
        {
          "step": "log_localization_llm",
          "role": "assistant",
          "content": """{"failed_module": "emqx_client", "failed_component": "N/A", "error_location": "N/A", "explanation": "verified_no_issue", "evidence": ["日志行：'[EMQXClient] publishing to topic: robot/robot_001/control' - MQTT发布操作执行成功", "日志行：'[EMQXClient] wait_for_callback timeout=5000ms' - 回调等待机制正常运作", "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - 错误信息明确指出是设备无响应，而非MQTT通信失败"], "confidence": 0.0}""",
          "timestamp": "2026-05-22T15:31:30.514678"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: emqx_client

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:31:58.107920"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{"capability": null, "end_signal": true, "reason": "已执行过 log_localization，emqx_client 模块本身执行正常（成功发布 MQTT 消息到 topic robot/robot_001/control），仅在 wait_for_callback 阶段超时导致设备无响应。该症状指向设备端或上游连接问题，不属于 emqx_client 模块自身的根因缺陷，置信度低，应结束分支进入 Reflection"}""",
          "timestamp": "2026-05-22T15:31:58.107945"
        },
        {
          "step": "reflection_llm",
          "role": "user",
          "content": """## 任务

你是当前诊断分支的反思器。你的唯一任务是：基于分支内已执行的能力结果和失败日志，评估"问题根因是否属于当前分支的模块 emqx_client"。

你禁止执行任何其他判断，包括但不限于：
- 禁止决定是否发送通知
- 禁止决定整个诊断流程是否停止
- 禁止将其他模块指定为问题模块
- 禁止输出任何与 JSON 格式无关的思考过程或解释文字

## 当前分支模块

emqx_client

## 情景描述

用户说往前走但机器人没有反应

## 失败日志


# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 当前分支完整记忆（包含决策对话和能力执行结果）

## 上下文

- [llm_decision] assistant: {"capability": "log_localization", "end_signal": false, "reason": "日志显示 emqx_client 正常发布到 MQTT topic 并等待回调，但设备无响应导致超时。需要通过 log_localization 验证该模块是否存在独立代码异常，或仅是下游设备无响应的症状"}
- [log_localization_llm] assistant: {"failed_module": "emqx_client", "failed_component": "N/A", "error_location": "N/A", "explanation": "verified_no_issue", "evidence": ["日志行：'[EMQXClient] publishing to topic: robot/robot_001/control' - MQTT发布操作执行成功", "日志行：'[EMQXClient] wait_for_callback timeout=5000ms' - 回调等待机制正常运作", "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - 错误信息明确指出是设备无响应，而非MQTT通信失败"], "confidence": 0.0}
- [llm_decision] assistant: {"capability": null, "end_signal": true, "reason": "已执行过 log_localization，emqx_client 模块本身执行正常（成功发布 MQTT 消息到 topic robot/robot_001/control），仅在 wait_for_callback 阶段超时导致设备无响应。该症状指向设备端或上游连接问题，不属于 emqx_client 模块自身的根因缺陷，置信度低，应结束分支进入 Reflection"}

## 能力使用

- ✅ log_localization (confidence=0.00)
  failed_module: emqx_client
  explanation: verified_no_issue


## 反思要求

1. **评估问题归属**：基于上述完整分支记忆（包括 Agent 的决策过程、各能力执行结果、对话历史）和失败日志，严格评估"问题根因是否属于 emqx_client 模块"。判断时必须同时依据：
   - 失败日志中该模块的直接异常记录
   - 分支内已执行能力（如 log_localization / code_localization）对该模块的具体分析结论
   禁止仅凭直觉或情景描述打分，禁止考虑其他分支模块的表现。

2. **给出置信度**：confidence 必须是 0.0-1.0 的纯数字，表示"**问题根因在该模块**"的可信程度，不是"该模块工作质量评分"。
   - 模块确实有问题 → confidence 越高越确信（0.5~1.0）
   - 模块没有问题 → confidence 必须接近 0.0，表示"问题在该模块"的概率为零
   严禁出现"模块无问题"但 confidence 很高（如 >0.5）的矛盾输出。
   打分规则：
   - 0.90-1.00：日志和分支能力均明确指向该模块存在根因，证据链完整
   - 0.70-0.89：日志或分支能力有较强的指向性，但存在少量可解释的异常
   - 0.40-0.69：证据模棱两可，既有支持也有反驳的线索
   - 0.10-0.39：证据倾向于排除该模块，但无法完全确认
   - 0.00-0.09：日志和分支能力均表明该模块无问题，或问题已被充分排除

   **上下游影响规则（必须严格遵守）**：
   Pipeline 顺序：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。
   - 如果当前模块的异常可以被更上游模块的错误**完全解释**（如上游输出错误数据导致当前模块处理失败），则当前模块的 confidence 必须 ≤ 0.3，因为这是"症状"不是"根因"
   - 只有当当前模块存在**独立缺陷**（与上游无关）时，confidence 才应 ≥ 0.5
   - 严禁因为"当前模块确实报错了"就给高分——必须判断报错是自身缺陷还是上游污染导致的症状

3. **列出关键证据**：evidence 数组中的每条证据必须引用日志原文或能力执行结果中的具体信息（如具体的错误日志行、具体的组件名、具体的根因描述），禁止输出空洞的概括性语句（如"模块表现异常"、"日志显示有问题"）。

## 输出格式

必须且只返回一个纯 JSON 对象，禁止包含 Markdown 代码块标记（```json）、解释文字、思考过程或任何其他内容：

{
  "module": "emqx_client",
  "confidence": 0.0-1.0,
  "explanation": "为什么问题属于或不属于当前模块",
  "evidence": ["证据1", "证据2"]
}

## Few-Shot 示例

### 示例 1：确认当前模块有问题（独立根因）
输入分支模块：intent_classifier
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"，"⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令"
分支能力结论：log_localization 定位到 failed_component="裁决"

输出：
{
  "module": "intent_classifier",
  "confidence": 0.92,
  "explanation": "日志和分支能力均明确指向该模块的裁决组件：rule_fast(0.8)和llm_cmd(0.7)均识别为指令，但裁决仍输出闲聊intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'",
    "log_localization 结果：failed_component='裁决'"
  ]
}

### 示例 2：排除当前模块（自身无问题）
输入分支模块：command_store
日志证据："[CommandStoreClient] vector_search k=5 score=0.72"，"✅ ES搜索成功: 匹配 cmd_001"
分支能力结论：log_localization 返回 explanation="verified_no_issue"

输出：
{
  "module": "command_store",
  "confidence": 0.02,
  "explanation": "日志显示 vector_search 成功匹配 cmd_001，ES搜索无异常；log_localization 已验证该模块无问题",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'",
    "log_localization 结果：explanation='verified_no_issue'"
  ]
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入分支模块：parameter_extractor
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"（上游已误判），"[ParameterExtractor] regex='往<方向>走' no match in '往前走'"
分支能力结论：log_localization 定位到 regex_match 失败

输出：
{
  "module": "parameter_extractor",
  "confidence": 0.18,
  "explanation": "当前模块的 regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。上游错误导致当前模块接收到了异常输入，regex 匹配失败是症状而非独立根因。根因在上游 intent_classifier 的裁决组件。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游在阶段1已误判为闲聊",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 下游收到异常输入 → regex 匹配失败是症状，不是独立缺陷"
  ]
}

**重要**：示例3 中 confidence 为 0.18（低分），因为当前模块的异常可以被更上游的错误完全解释。严禁因为"当前模块确实出现了报错"就给高分。

## 重要约束

1. module 字段必须严格等于 "emqx_client"，任何其他值（包括大小写差异）均视为无效输出，会导致解析失败。
2. confidence 必须是 0.0 到 1.0 之间的纯数字，禁止使用字符串形式，禁止超出此范围。
3. 禁止输出 decision、final_module、stop、notify、action 等额外字段。
4. 禁止在 JSON 中输出"停止通知"、"跳过 Finalization"、"通知某某"等流程控制文字。
4. 只反思当前模块 emqx_client，禁止在 explanation 或 evidence 中与其他模块进行比较或引用其他模块的结论。
6. 禁止在 JSON 前后输出任何 ``` 标记或自然语言解释。""",
          "timestamp": "2026-05-22T15:32:19.301253"
        },
        {
          "step": "reflection_llm",
          "role": "assistant",
          "content": """{
  "module": "emqx_client",
  "confidence": 0.02,
  "explanation": "emqx_client 模块的 MQTT 发布操作已成功执行，回调等待机制也正常运作。日志中的错误信息'设备无响应'明确指向下游设备或网络层问题，而非该模块的代码缺陷。log_localization 已验证该模块无问题（explanation='verified_no_issue'），confidence=0.0 表示确认模块本身无根因缺陷。整个 MQTT 通信链路的发送端工作正常，超时原因是设备端未在合理时间内响应。",
  "evidence": [
    "日志行：'[EMQXClient] publishing to topic: robot/robot_001/control' - MQTT发布操作执行成功",
    "日志行：'[EMQXClient] wait_for_callback timeout=5000ms' - 回调等待机制正常运作",
    "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - 错误信息明确指出是设备无响应，而非MQTT通信失败",
    "log_localization 结果：explanation='verified_no_issue'，confidence=0.0"
  ]
}""",
          "timestamp": "2026-05-22T15:32:19.301277"
        }
      ],
      "iteration": [
        {
          "step": "log_localization",
          "evidence": {
            "failed_module": "emqx_client",
            "failed_component": "N/A",
            "error_location": "N/A",
            "explanation": "verified_no_issue",
            "evidence": [
              "日志行：'[EMQXClient] publishing to topic: robot/robot_001/control' - MQTT发布操作执行成功",
              "日志行：'[EMQXClient] wait_for_callback timeout=5000ms' - 回调等待机制正常运作",
              "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - 错误信息明确指出是设备无响应，而非MQTT通信失败"
            ],
            "confidence": 0.0
          },
          "success": true,
          "target_module": "emqx_client",
          "confidence": 0.0,
          "timestamp": "2026-05-22T15:31:30.514686"
        },
        {
          "step": "reflection",
          "evidence": {
            "confidence": 0.02,
            "explanation": "emqx_client 模块的 MQTT 发布操作已成功执行，回调等待机制也正常运作。日志中的错误信息'设备无响应'明确指向下游设备或网络层问题，而非该模块的代码缺陷。log_localization 已验证该模块无问题（explanation='verified_no_issue'），confidence=0.0 表示确认模块本身无根因缺陷。整个 MQTT 通信链路的发送端工作正常，超时原因是设备端未在合理时间内响应。",
            "evidence": [
              "日志行：'[EMQXClient] publishing to topic: robot/robot_001/control' - MQTT发布操作执行成功",
              "日志行：'[EMQXClient] wait_for_callback timeout=5000ms' - 回调等待机制正常运作",
              "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应' - 错误信息明确指出是设备无响应，而非MQTT通信失败",
              "log_localization 结果：explanation='verified_no_issue'，confidence=0.0"
            ],
            "module": "emqx_client"
          },
          "success": true,
          "target_module": "emqx_client",
          "confidence": 0.02,
          "timestamp": "2026-05-22T15:32:19.301287"
        }
      ]
    }
  },
  "bug_module": "intent_classifier",
  "owner_open_id": "ou_da6f41f726b64bd84672ca958390b282"
}