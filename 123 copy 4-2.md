{
  "meta": {
    "exported_at": "2026-05-22T15:42:32.484979",
    "created_at": "2026-05-22T15:00:13.325685"
  },
  "global_iteration": [
    {
      "step": "situation_analysis",
      "evidence": {
        "possible_modules": [
          {
            "module": "intent_classifier",
            "explanation": "三路模型中 rule_fast 识别为闲聊(intent=2, conf=0.8)，llm_light 识别为指令(intent=1, conf=0.65)，llm_cmd 也识别为指令(intent=1, conf=0.7)。裁决组件在存在矛盾的情况下选择置信度 0.42 的闲聊意图，而不是更高置信度(0.7)的指令意图。这导致指令被当作闲聊处理，虽然后续指令重写和ES搜索表面成功，但整体流程被错误引导。最终设备无响应的根因是意图误判导致的后续流程异常。",
            "confidence": 0.85
          },
          {
            "module": "parameter_extractor",
            "explanation": """regex="往<方向>走" 无法匹配用户输入"往前走"，导致参数提取失败回退默认值。"前"作为方向词应该被正则捕获，但模式未能识别。这是独立缺陷，但发生在意图分类误判之后，属于下游症状模块。""",
            "confidence": 0.45
          },
          {
            "module": "emqx_client",
            "explanation": "设备无响应表现为当前模块失败，但这是整个错误链条的症状结果而非独立根因。协议生成成功说明参数传递到了MQTT层，设备无响应可能因上游意图误判导致机器人未处于可接收指令的状态。",
            "confidence": 0.3
          }
        ]
      },
      "success": true,
      "target_module": "N/A",
      "confidence": 0.0,
      "timestamp": "2026-05-22T15:00:58.714438"
    },
    {
      "step": "global_reflection",
      "evidence": {
        "root_cause_module": "intent_classifier",
        "confidence": 0.94,
        "explanation": """根因：intent_classifier 的裁决组件存在严重缺陷，导致用户指令'往前走'被误判为闲聊意图(intent=2)，置信度仅0.42。该裁决逻辑失效是 Pipeline 中的首个异常点，位于最前端(阶段1)，直接污染了整个数据流。

关键证据：1) 日志'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'明确显示最终输出闲聊意图；2) 日志'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令'直接标注裁决逻辑失效；3) code_localization 指出 _weighted_vote 采用乘法融合(Π(1+w*conf))，rule_fast 权重1.0可压制其他子模块，导致两个LLM(llm_light: conf=0.65, llm_cmd: conf=0.7)都判定为指令但被最终错误覆盖。

因果链分析：1) intent_classifier 裁决输出闲聊(intent=2)→ 2) command_store 基于闲聊意图执行 ES 双搜(但指令匹配 cmd_001 本身无错) → 3) parameter_extractor 正则'往<方向>走'无法匹配'往前走'，这是因为输入是闲聊意图而非移动指令的参数提取需求，回退机制虽补救成功但掩盖了真正的上游问题 → 4) protocol_builder 基于错误意图生成协议 → 5) emqx_client 收到协议后尝试 MQTT 发布，最终超时失败。

被判定为症状的模块：parameter_extractor 的正则匹配失败和回退行为是上游裁决错误的连锁反应；emqx_client 的 MQTT 超时虽为独立代码缺陷，但在本因果链中因上游错误意图触发了最终执行失败。""",
        "symptom_modules": [
          "parameter_extractor",
          "emqx_client"
        ]
      },
      "success": true,
      "target_module": "intent_classifier",
      "confidence": 0.94,
      "timestamp": "2026-05-22T15:42:31.470945"
    }
  ],
  "possible_modules": [
    {
      "module": "intent_classifier",
      "confidence": 2.75,
      "explanation": "三路模型中 rule_fast 识别为闲聊(intent=2, conf=0.8)，llm_light 识别为指令(intent=1, conf=0.65)，llm_cmd 也识别为指令(intent=1, conf=0.7)。裁决组件在存在矛盾的情况下选择置信度 0.42 的闲聊意图，而不是更高置信度(0.7)的指令意图。这导致指令被当作闲聊处理，虽然后续指令重写和ES搜索表面成功，但整体流程被错误引导。最终设备无响应的根因是意图误判导致的后续流程异常。"
    },
    {
      "module": "parameter_extractor",
      "confidence": 0.5700000000000001,
      "explanation": """regex="往<方向>走" 无法匹配用户输入"往前走"，导致参数提取失败回退默认值。"前"作为方向词应该被正则捕获，但模式未能识别。这是独立缺陷，但发生在意图分类误判之后，属于下游症状模块。"""
    },
    {
      "module": "emqx_client",
      "confidence": 1.22,
      "explanation": "设备无响应表现为当前模块失败，但这是整个错误链条的症状结果而非独立根因。协议生成成功说明参数传递到了MQTT层，设备无响应可能因上游意图误判导致机器人未处于可接收指令的状态。"
    }
  ],
  "global_context": [],
  "branch_memories": {
    "intent_classifier": {
      "context": [
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
尚未执行任何 capability

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:01:12.628477"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中 intent_classifier 存在明显的模块行为矛盾：IntentEnsemble 最终裁决 intent=2(闲聊) conf=0.42，但子模块 rule_fast 和 llm_cmd 都显示命中指令而非闲聊，且日志明确标注「裁决失败」。这种矛盾表明模块的裁决逻辑可能存在缺陷，需要通过 log_localization 验证具体问题点"
}""",
          "timestamp": "2026-05-22T15:01:12.628488"
        },
        {
          "step": "log_localization_llm",
          "role": "user",
          "content": """## 任务

你是机器人控制 Pipeline 的日志分析专家。验证可疑模块是否存在问题，定位到具体的组件。

## 待验证模块

模块名称: intent_classifier
模块置信度: 0.0
怀疑原因: 

模块内部组件: classify, ensemble, 裁决, conf_threshold

**你必须只分析这个模块，不要分析其他模块。**
**通过日志验证该模块是否确实存在问题，并定位到具体的组件。**

## 当前分支记忆

## 上下文

- [llm_decision] assistant: {
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中 intent_classifier 存在明显的模块行为矛盾：IntentEnsemble 最终裁决 intent=2(闲聊) conf=0.42，但子模块 rule_fast 和 llm_cmd 都显示命中指令而非闲聊，且日志明确标注「裁决失败」。这种矛盾表明模块的裁决逻辑可能存在缺陷，需要通过 log_localization 验证具体问题点"
}


## 输入信息
情景描述: 用户说往前走但机器人没有反应
失败日志:

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 分析要求

1. **只分析上述待验证模块**：不要分析其他模块，即使日志中提到了其他模块
2. **分析该模块的日志表现**：查看该模块在日志中的输出
3. **定位问题组件**：找出该模块中具体哪个组件出了问题
4. **如果该模块没有问题**：返回 explanation = "verified_no_issue"
5. **计算置信度**：根据日志证据的充分性和问题定位的确定性，给出 confidence (0.0~1.0)

## 输出格式

必须返回纯 JSON 对象，禁止包含任何 Markdown 标记、思考过程或解释文字：

{
  "failed_module": "intent_classifier",
  "failed_component": "该模块内的具体组件名称",
  "error_location": "日志中的具体错误位置",
  "explanation": "根本原因分析，或 'verified_no_issue' 表示该模块没有问题",
  "evidence": ["日志证据1", "日志证据2"],
  "confidence": 确认是当前模块出问题的置信度，0.0~1.0 的数字。
}

## Few-Shot 示例

### 示例 1：模块自身存在问题（独立根因）
输入：待验证模块=intent_classifier，模块组件=["classify", "ensemble", "裁决", "conf_threshold"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
[Ensemble] trace rule_fast: intent=2 conf=0.8
[Ensemble] trace llm_cmd: intent=1 conf=0.7
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
```
输出：
{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件在 rule_fast(0.8)和 llm_cmd(0.7)都识别为指令时仍输出闲聊 intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'"
  ],
  "confidence": 0.9
}

### 示例 2：模块没有问题
输入：待验证模块=command_store，模块组件=["vector_search", "text_search", "merge_results"]
日志：
```
[CommandStoreClient] vector_search k=5 score=0.72
[CommandStoreClient] matched_command: {"id": "cmd_001"}
✅ ES搜索成功: 匹配 cmd_001
```
输出：
{
  "failed_module": "command_store",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'"
  ],
  "confidence": 0.0
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入：待验证模块=parameter_extractor，模块组件=["regex_match", "capture", "default_fallback"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"
```
输出：
{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex="往<方向>走" no match in "往前走"",
  "explanation": "regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。当前模块接收到的输入已被上游错误污染（'往前走'被误判为闲聊意图，导致 regex 模式不匹配）。这是上游错误导致的症状，不是 parameter_extractor 的独立根因。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游意图分类已误判",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 当前模块收到异常输入 → regex 匹配失败是症状而非独立缺陷"
  ],
  "confidence": 0.2
}

**重要**：示例3 中 confidence 为 0.2（低分），因为虽然当前模块确实出现了异常，但其异常可以被更上游的错误完全解释。根因是上游的 intent_classifier，不是当前模块。

## confidence 语义（必须严格遵守）

confidence 表示"**日志证据支持'问题根因在该模块'的置信度**"，不是"该模块工作质量评分"。

- 模块**确实有问题** → confidence 越高表示越确信（0.5~1.0）
- 模块**没有问题**（explanation = "verified_no_issue"）→ confidence **必须为 0.0**，表示"问题在该模块"的概率为零

严禁出现 explanation="verified_no_issue" 但 confidence > 0 的矛盾输出。

## 重要约束

1. **只输出 JSON**：禁止输出 ```json、```、解释文字或思考过程
2. **failed_module**：必须返回 "intent_classifier"
3. **explanation**：如果该模块没有问题，必须返回 "verified_no_issue"，**此时 confidence 必须为 0.0**
4. **confidence**：必须返回 0.0~1.0 的数字，语义是"问题根因在该模块"的置信度
5. **evidence**：必须列出支撑 explanation 的具体日志证据，不能为空
5. **只分析待验证模块**：不要分析其他模块
6. **所有字段必须完整**：输出必须包含全部 5 个字段，缺一不可""",
          "timestamp": "2026-05-22T15:04:17.341650"
        },
        {
          "step": "log_localization_llm",
          "role": "assistant",
          "content": """{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件存在严重逻辑缺陷。从日志可见：rule_fast输出intent=2 conf=0.8，llm_light输出intent=1 conf=0.65，llm_cmd输出intent=1 conf=0.7。三个子模块中两个指向intent=1(指令)，理应多数裁决为intent=1，但最终裁决结果却是intent=2(闲聊)且置信度只有0.42。更关键的是，最终置信度0.42甚至低于子模块llm_light(0.65)和llm_cmd(0.7)，表明裁决逻辑在整合多个子模块输出时严重失效，未能正确执行多数投票或加权聚合。日志明确标注'裁决失败'，确认了这一根因缺陷。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms' - 最终裁决输出闲聊意图",
    "日志行：'[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']' - rule_fast判定为闲聊",
    "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']' - llm_light判定为指令",
    "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']' - llm_cmd判定为指令",
    "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决失败，2/3子模块指向指令但最终输出闲聊"
  ],
  "confidence": 0.95
}""",
          "timestamp": "2026-05-22T15:04:17.341659"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:04:22.562484"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """```json
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志显示 intent_classifier 的裁决存在明显矛盾（裁决 intent=2 conf=0.42，但 rule_fast=0.8 和 llm_cmd=0.7 都指向 intent=1），且最终置信度偏低。这种裁决逻辑是否正确、以及矛盾结果如何影响后续流程，需要深入代码才能确认。已执行过 log_localization 但无法定位具体代码逻辑问题。"
}
```""",
          "timestamp": "2026-05-22T15:04:22.562499"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization, code_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:18:06.932141"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{"capability":"code_localization","end_signal":false,"reason":"从日志可见关键矛盾：rule_fast、llm_light、llm_cmd 三个 tracer 都输出 intent=1（指令），但最终 IntentEnsemble 裁决为 intent=2（闲聊）。这种裁决结果与多个 tracer 共识相悖的异常，需要深入代码层面检查 Ensemble 的裁决逻辑和权重配置，才能确认是否为当前模块的独立缺陷。"}""",
          "timestamp": "2026-05-22T15:18:06.932156"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: intent_classifier

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization, code_localization, code_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:25:38.677579"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{
  "capability": null,
  "end_signal": true,
  "reason": "意图分类阶段存在明显的 Ensemble 裁决矛盾：rule_fast (conf=0.8) 和 llm_cmd (conf=0.7) 都判定为指令，但最终裁决为闲聊(conf=0.42)，这是明确的模块内部决策错误。当前模块置信度 0.8 已达到确认根因标准，应结束分支进入 Reflection。"
}""",
          "timestamp": "2026-05-22T15:25:38.677603"
        },
        {
          "step": "reflection_llm",
          "role": "user",
          "content": """## 任务

你是当前诊断分支的反思器。你的唯一任务是：基于分支内已执行的能力结果和失败日志，评估"问题根因是否属于当前分支的模块 intent_classifier"。

你禁止执行任何其他判断，包括但不限于：
- 禁止决定是否发送通知
- 禁止决定整个诊断流程是否停止
- 禁止将其他模块指定为问题模块
- 禁止输出任何与 JSON 格式无关的思考过程或解释文字

## 当前分支模块

intent_classifier

## 情景描述

用户说往前走但机器人没有反应

## 失败日志


# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 当前分支完整记忆（包含决策对话和能力执行结果）

## 上下文

- [llm_decision] assistant: {
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中 intent_classifier 存在明显的模块行为矛盾：IntentEnsemble 最终裁决 intent=2(闲聊) conf=0.42，但子模块 rule_fast 和 llm_cmd 都显示命中指令而非闲聊，且日志明确标注「裁决失败」。这种矛盾表明模块的裁决逻辑可能存在缺陷，需要通过 log_localization 验证具体问题点"
}
- [log_localization_llm] assistant: {
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件存在严重逻辑缺陷。从日志可见：rule_fast输出intent=2 conf=0.8，llm_light输出intent=1 conf=0.65，llm_cmd输出intent=1 conf=0.7。三个子模块中两个指向intent=1(指令)，理应多数裁决为intent=1，但最终裁决结果却是intent=2(闲聊)且置信度只有0.42。更关键的是，最终置信度0.42甚至低于子模块llm_light(0.65)和llm_cmd(0.7)，表明裁决逻辑在整合多个子模块输出时严重失效，未能正确执行多数投票或加权聚合。日志明确标注'裁决失败'，确认了这一根因缺陷。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms' - 最终裁决输出闲聊意图",
    "日志行：'[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']' - rule_fast判定为闲聊",
    "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']' - llm_light判定为指令",
    "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']' - llm_cmd判定为指令",
    "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决失败，2/3子模块指向指令但最终输出闲聊"
  ],
  "confidence": 0.95
}
- [llm_decision] assistant: ```json
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志显示 intent_classifier 的裁决存在明显矛盾（裁决 intent=2 conf=0.42，但 rule_fast=0.8 和 llm_cmd=0.7 都指向 intent=1），且最终置信度偏低。这种裁决逻辑是否正确、以及矛盾结果如何影响后续流程，需要深入代码才能确认。已执行过 log_localization 但无法定位具体代码逻辑问题。"
}
```
- [llm_decision] assistant: {"capability":"code_localization","end_signal":false,"reason":"从日志可见关键矛盾：rule_fast、llm_light、llm_cmd 三个 tracer 都输出 intent=1（指令），但最终 IntentEnsemble 裁决为 intent=2（闲聊）。这种裁决结果与多个 tracer 共识相悖的异常，需要深入代码层面检查 Ensemble 的裁决逻辑和权重配置，才能确认是否为当前模块的独立缺陷。"}
- [llm_decision] assistant: {
  "capability": null,
  "end_signal": true,
  "reason": "意图分类阶段存在明显的 Ensemble 裁决矛盾：rule_fast (conf=0.8) 和 llm_cmd (conf=0.7) 都判定为指令，但最终裁决为闲聊(conf=0.42)，这是明确的模块内部决策错误。当前模块置信度 0.8 已达到确认根因标准，应结束分支进入 Reflection。"
}

## 能力使用

- ✅ log_localization (confidence=0.95)
  failed_module: intent_classifier
  explanation: 裁决组件存在严重逻辑缺陷。从日志可见：rule_fast输出intent=2 conf=0.8，llm_light输出intent=1 conf=0.65，llm_cmd输出intent=1 conf=0.7。三个子模块中两个指向intent=1(指令)，理应多数裁决为intent=1，但最终裁决结果却是intent=2(闲聊)且置信度只有0.42。更关键的是，最终置信度0.42甚至低于子模块llm_light(0.65)和llm_cmd(0.7)，表明裁决逻辑在整合多个子模块输出时严重失效，未能正确执行多数投票或加权聚合。日志明确标注'裁决失败'，确认了这一根因缺陷。
- ✅ code_localization (confidence=0.88)
  module: intent_classifier
  explanation: 该模块存在两处关键缺陷导致意图误判：1) _rule_fast 匹配 chat_patterns 后立即返回 intent=2（闲聊），不给 LLM 子模块纠正机会，'往前走'可能被闲聊规则误拦截；2) _weighted_vote 使用乘法融合而非多数投票，当子模块分歧时可能产生反直觉结果（如 llm_light 和 llm_cmd 都判定为指令，但最终输出闲聊）。此外 llm_cmd 的二元映射（非1即2）和超时回退机制（默认回退到闲聊）进一步增加了误判风险。
- ✅ code_localization (confidence=0.92)
  module: intent_classifier
  explanation: intent_classifier 模块存在两处关键裁决逻辑缺陷导致意图误判。第一，_rule_fast 在匹配 chat_patterns 后于第163-165行立即返回 intent=2（闲聊），且 classify 方法第406-409行对 rule_fast 结果进行短路直接返回，完全跳过 LLM 子模块的校验，'往前走'等指令可能被闲聊规则误拦截。第二，_weighted_vote 第362-391行采用乘法融合（score = Π(1 + w * conf)）而非多数投票，rule_fast 和 task_lock 的权重被硬编码为 1.0（否决级），当 rule_fast 输出高置信度闲聊时，其 score=1.8 可压制 llm_light 与 llm_cmd 两个子模块联合产生的约 1.609 得分，导致两个 LLM 都判定为指令的情况下仍输出闲聊。日志中 llm_light 与 llm_cmd 均命中指令但裁决结果为闲聊，与该缺陷模式完全吻合。


## 反思要求

1. **评估问题归属**：基于上述完整分支记忆（包括 Agent 的决策过程、各能力执行结果、对话历史）和失败日志，严格评估"问题根因是否属于 intent_classifier 模块"。判断时必须同时依据：
   - 失败日志中该模块的直接异常记录
   - 分支内已执行能力（如 log_localization / code_localization）对该模块的具体分析结论
   禁止仅凭直觉或情景描述打分，禁止考虑其他分支模块的表现。

2. **给出置信度**：confidence 必须是 0.0-1.0 的纯数字，表示"**问题根因在该模块**"的可信程度，不是"该模块工作质量评分"。
   - 模块确实有问题 → confidence 越高越确信（0.5~1.0）
   - 模块没有问题 → confidence 必须接近 0.0，表示"问题在该模块"的概率为零
   严禁出现"模块无问题"但 confidence 很高（如 >0.5）的矛盾输出。
   打分规则：
   - 0.90-1.00：日志和分支能力均明确指向该模块存在根因，证据链完整
   - 0.70-0.89：日志或分支能力有较强的指向性，但存在少量可解释的异常
   - 0.40-0.69：证据模棱两可，既有支持也有反驳的线索
   - 0.10-0.39：证据倾向于排除该模块，但无法完全确认
   - 0.00-0.09：日志和分支能力均表明该模块无问题，或问题已被充分排除

   **上下游影响规则（必须严格遵守）**：
   Pipeline 顺序：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。
   - 如果当前模块的异常可以被更上游模块的错误**完全解释**（如上游输出错误数据导致当前模块处理失败），则当前模块的 confidence 必须 ≤ 0.3，因为这是"症状"不是"根因"
   - 只有当当前模块存在**独立缺陷**（与上游无关）时，confidence 才应 ≥ 0.5
   - 严禁因为"当前模块确实报错了"就给高分——必须判断报错是自身缺陷还是上游污染导致的症状

3. **列出关键证据**：evidence 数组中的每条证据必须引用日志原文或能力执行结果中的具体信息（如具体的错误日志行、具体的组件名、具体的根因描述），禁止输出空洞的概括性语句（如"模块表现异常"、"日志显示有问题"）。

## 输出格式

必须且只返回一个纯 JSON 对象，禁止包含 Markdown 代码块标记（```json）、解释文字、思考过程或任何其他内容：

{
  "module": "intent_classifier",
  "confidence": 0.0-1.0,
  "explanation": "为什么问题属于或不属于当前模块",
  "evidence": ["证据1", "证据2"]
}

## Few-Shot 示例

### 示例 1：确认当前模块有问题（独立根因）
输入分支模块：intent_classifier
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"，"⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令"
分支能力结论：log_localization 定位到 failed_component="裁决"

输出：
{
  "module": "intent_classifier",
  "confidence": 0.92,
  "explanation": "日志和分支能力均明确指向该模块的裁决组件：rule_fast(0.8)和llm_cmd(0.7)均识别为指令，但裁决仍输出闲聊intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'",
    "log_localization 结果：failed_component='裁决'"
  ]
}

### 示例 2：排除当前模块（自身无问题）
输入分支模块：command_store
日志证据："[CommandStoreClient] vector_search k=5 score=0.72"，"✅ ES搜索成功: 匹配 cmd_001"
分支能力结论：log_localization 返回 explanation="verified_no_issue"

输出：
{
  "module": "command_store",
  "confidence": 0.02,
  "explanation": "日志显示 vector_search 成功匹配 cmd_001，ES搜索无异常；log_localization 已验证该模块无问题",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'",
    "log_localization 结果：explanation='verified_no_issue'"
  ]
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入分支模块：parameter_extractor
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"（上游已误判），"[ParameterExtractor] regex='往<方向>走' no match in '往前走'"
分支能力结论：log_localization 定位到 regex_match 失败

输出：
{
  "module": "parameter_extractor",
  "confidence": 0.18,
  "explanation": "当前模块的 regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。上游错误导致当前模块接收到了异常输入，regex 匹配失败是症状而非独立根因。根因在上游 intent_classifier 的裁决组件。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游在阶段1已误判为闲聊",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 下游收到异常输入 → regex 匹配失败是症状，不是独立缺陷"
  ]
}

**重要**：示例3 中 confidence 为 0.18（低分），因为当前模块的异常可以被更上游的错误完全解释。严禁因为"当前模块确实出现了报错"就给高分。

## 重要约束

1. module 字段必须严格等于 "intent_classifier"，任何其他值（包括大小写差异）均视为无效输出，会导致解析失败。
2. confidence 必须是 0.0 到 1.0 之间的纯数字，禁止使用字符串形式，禁止超出此范围。
3. 禁止输出 decision、final_module、stop、notify、action 等额外字段。
4. 禁止在 JSON 中输出"停止通知"、"跳过 Finalization"、"通知某某"等流程控制文字。
4. 只反思当前模块 intent_classifier，禁止在 explanation 或 evidence 中与其他模块进行比较或引用其他模块的结论。
6. 禁止在 JSON 前后输出任何 ``` 标记或自然语言解释。""",
          "timestamp": "2026-05-22T15:25:45.772235"
        },
        {
          "step": "reflection_llm",
          "role": "assistant",
          "content": """{
  "module": "intent_classifier",
  "confidence": 0.96,
  "explanation": "日志和分支能力均明确指向该模块存在独立根因。失败日志显示 IntentEnsemble 裁决 intent=2(闲聊) conf=0.42，但 llm_light(intent=1 conf=0.65)和 llm_cmd(intent=1 conf=0.7)两个子模块都判定为指令，日志明确标注'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令'。这是裁决组件的独立决策错误，与上游 rejection_classifier 完全无关——rejection_classifier 是 Pipeline 第一关，如果它误判了闲聊意图，应该在更早阶段被拦截，而非让'往前走'进入意图分类阶段。两个 code_localization 能力进一步定位到代码层面的具体缺陷：_rule_fast 短路直接返回 intent=2 跳过 LLM 校验，以及 _weighted_vote 采用乘法融合导致 rule_fast 的否决权重压制了其他子模块的多数共识。根因明确属于 intent_classifier 模块的裁决逻辑。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 最终裁决输出闲聊意图而非指令",
    "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 reasons=['llm_light']' - llm_light 判定为指令",
    "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 reasons=['llm_cmd']' - llm_cmd 判定为指令",
    "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决逻辑失效",
    "log_localization 结果：failed_module='intent_classifier', failed_component='裁决', confidence=0.95",
    "code_localization 结果：指出 _weighted_vote 采用乘法融合(Π(1+w*conf))而非多数投票，rule_fast 权重1.0可压制其他子模块，导致两个LLM都判定为指令但最终输出闲聊",
    "因果关系：Pipeline 顺序为 rejection_classifier → intent_classifier → ...，'往前走'能进入意图分类阶段说明 upstream 无误判，根因独立存在于 intent_classifier 的裁决组件"
  ]
}""",
          "timestamp": "2026-05-22T15:25:45.772249"
        }
      ],
      "iteration": [
        {
          "step": "log_localization",
          "evidence": {
            "failed_module": "intent_classifier",
            "failed_component": "裁决",
            "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
            "explanation": "裁决组件存在严重逻辑缺陷。从日志可见：rule_fast输出intent=2 conf=0.8，llm_light输出intent=1 conf=0.65，llm_cmd输出intent=1 conf=0.7。三个子模块中两个指向intent=1(指令)，理应多数裁决为intent=1，但最终裁决结果却是intent=2(闲聊)且置信度只有0.42。更关键的是，最终置信度0.42甚至低于子模块llm_light(0.65)和llm_cmd(0.7)，表明裁决逻辑在整合多个子模块输出时严重失效，未能正确执行多数投票或加权聚合。日志明确标注'裁决失败'，确认了这一根因缺陷。",
            "evidence": [
              "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms' - 最终裁决输出闲聊意图",
              "日志行：'[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']' - rule_fast判定为闲聊",
              "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']' - llm_light判定为指令",
              "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']' - llm_cmd判定为指令",
              "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决失败，2/3子模块指向指令但最终输出闲聊"
            ],
            "confidence": 0.95
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.95,
          "timestamp": "2026-05-22T15:04:17.341668"
        },
        {
          "step": "code_localization",
          "evidence": {
            "module": "intent_classifier",
            "confidence": 0.88,
            "explanation": "该模块存在两处关键缺陷导致意图误判：1) _rule_fast 匹配 chat_patterns 后立即返回 intent=2（闲聊），不给 LLM 子模块纠正机会，'往前走'可能被闲聊规则误拦截；2) _weighted_vote 使用乘法融合而非多数投票，当子模块分歧时可能产生反直觉结果（如 llm_light 和 llm_cmd 都判定为指令，但最终输出闲聊）。此外 llm_cmd 的二元映射（非1即2）和超时回退机制（默认回退到闲聊）进一步增加了误判风险。",
            "evidence": [
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  152,
                  163,
                  164,
                  165,
                  406,
                  407,
                  408,
                  409
                ],
                "code_snippets": [
                  "def _rule_fast(self, text: str, feature: IntentFeatureResult) -> Optional[IntentCandidate]:",
                  """chat_hit = self._match_patterns(text, chat_patterns)
    if chat_hit:
        return chat_hit""",
                  """rule_hit = self._rule_fast(text, feature_result)
    if rule_hit:
        logger.info(f"[Ensemble] RuleFast 命中: {rule_hit}")
        return self._to_intent_result(rule_hit, [rule_hit])"""
                ]
              },
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  362,
                  366,
                  369,
                  370,
                  371,
                  383,
                  384,
                  386,
                  389,
                  391
                ],
                "code_snippets": [
                  "def _weighted_vote(self, candidates: List[IntentCandidate]) -> EnsembleDecision:",
                  "# 乘法融合：score = Π(1 + w * conf)",
                  """for c in candidates:
    if c.model_tag == "llm_cmd":
        w = self.w_cmd_hit if c.intent_code == 1 else self.w_cmd_miss""",
                  """combined = 1.0 + max(0.0, w * c.confidence)
    score_map[c.intent_code] = score_map.get(c.intent_code, 1.0) * combined""",
                  "best_intent = max(score_map.items(), key=lambda kv: kv[1])[0]",
                  "conf = 1.0 - math.exp(-(best_score - 1.0))",
                  "return EnsembleDecision(intent_code=best_intent, confidence=min(1.0, conf), decision_trace=trace)"
                ]
              },
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  279,
                  290,
                  291,
                  292,
                  300,
                  303,
                  304,
                  307
                ],
                "code_snippets": [
                  "async def _llm_command(self, text: str) -> Optional[IntentCandidate]:",
                  """# 只关心是否指令：1=指令，其他视为非指令(2)
    intent_code = 1 if result.intent_code == 1 else 2
    conf = 0.7 if intent_code == 1 else 0.4""",
                  """except asyncio.TimeoutError:
    logger.warning("[LLM-Command] 超时")
    return IntentCandidate(intent_code=2, confidence=0.1, reasons=["timeout"], cost_ms=cost_ms, model_tag="llm_cmd")""",
                  """except Exception as e:
    logger.error(f"[LLM-Command] 调用失败: {e}")
    return IntentCandidate(intent_code=2, confidence=0.1, reasons=["error"], cost_ms=cost_ms, model_tag="llm_cmd")"""
                ]
              }
            ]
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.88,
          "timestamp": "2026-05-22T15:17:47.127742"
        },
        {
          "step": "code_localization",
          "evidence": {
            "module": "intent_classifier",
            "confidence": 0.92,
            "explanation": "intent_classifier 模块存在两处关键裁决逻辑缺陷导致意图误判。第一，_rule_fast 在匹配 chat_patterns 后于第163-165行立即返回 intent=2（闲聊），且 classify 方法第406-409行对 rule_fast 结果进行短路直接返回，完全跳过 LLM 子模块的校验，'往前走'等指令可能被闲聊规则误拦截。第二，_weighted_vote 第362-391行采用乘法融合（score = Π(1 + w * conf)）而非多数投票，rule_fast 和 task_lock 的权重被硬编码为 1.0（否决级），当 rule_fast 输出高置信度闲聊时，其 score=1.8 可压制 llm_light 与 llm_cmd 两个子模块联合产生的约 1.609 得分，导致两个 LLM 都判定为指令的情况下仍输出闲聊。日志中 llm_light 与 llm_cmd 均命中指令但裁决结果为闲聊，与该缺陷模式完全吻合。",
            "evidence": [
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  152,
                  163,
                  165,
                  406,
                  409
                ],
                "code_snippets": [
                  "def _rule_fast(self, text: str, feature: IntentFeatureResult) -> Optional[IntentCandidate]:",
                  "chat_hit = self._match_patterns(text, chat_patterns)",
                  "if chat_hit:",
                  "    return chat_hit",
                  "rule_hit = self._rule_fast(text, feature_result)",
                  "if rule_hit:",
                  """    logger.info(f"[Ensemble] RuleFast 命中: {rule_hit}")""",
                  "    return self._to_intent_result(rule_hit, [rule_hit])"
                ]
              },
              {
                "file_path": "super_agent/intent/ensemble.py",
                "line_numbers": [
                  362,
                  366,
                  379,
                  383,
                  384,
                  386,
                  389
                ],
                "code_snippets": [
                  "def _weighted_vote(self, candidates: List[IntentCandidate]) -> EnsembleDecision:",
                  "# 乘法融合：score = Π(1 + w * conf)",
                  """"rule_fast": 1.0,""",
                  """"task_lock": 1.0,""",
                  "combined = 1.0 + max(0.0, w * c.confidence)",
                  "score_map[c.intent_code] = score_map.get(c.intent_code, 1.0) * combined",
                  "best_intent = max(score_map.items(), key=lambda kv: kv[1])[0]",
                  "conf = 1.0 - math.exp(-(best_score - 1.0))"
                ]
              }
            ]
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.92,
          "timestamp": "2026-05-22T15:24:57.495764"
        },
        {
          "step": "reflection",
          "evidence": {
            "confidence": 0.96,
            "explanation": "日志和分支能力均明确指向该模块存在独立根因。失败日志显示 IntentEnsemble 裁决 intent=2(闲聊) conf=0.42，但 llm_light(intent=1 conf=0.65)和 llm_cmd(intent=1 conf=0.7)两个子模块都判定为指令，日志明确标注'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令'。这是裁决组件的独立决策错误，与上游 rejection_classifier 完全无关——rejection_classifier 是 Pipeline 第一关，如果它误判了闲聊意图，应该在更早阶段被拦截，而非让'往前走'进入意图分类阶段。两个 code_localization 能力进一步定位到代码层面的具体缺陷：_rule_fast 短路直接返回 intent=2 跳过 LLM 校验，以及 _weighted_vote 采用乘法融合导致 rule_fast 的否决权重压制了其他子模块的多数共识。根因明确属于 intent_classifier 模块的裁决逻辑。",
            "evidence": [
              "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 最终裁决输出闲聊意图而非指令",
              "日志行：'[Ensemble] trace llm_light: intent=1 conf=0.65 reasons=['llm_light']' - llm_light 判定为指令",
              "日志行：'[Ensemble] trace llm_cmd: intent=1 conf=0.7 reasons=['llm_cmd']' - llm_cmd 判定为指令",
              "日志行：'⚠️ 裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令' - 日志明确标注裁决逻辑失效",
              "log_localization 结果：failed_module='intent_classifier', failed_component='裁决', confidence=0.95",
              "code_localization 结果：指出 _weighted_vote 采用乘法融合(Π(1+w*conf))而非多数投票，rule_fast 权重1.0可压制其他子模块，导致两个LLM都判定为指令但最终输出闲聊",
              "因果关系：Pipeline 顺序为 rejection_classifier → intent_classifier → ...，'往前走'能进入意图分类阶段说明 upstream 无误判，根因独立存在于 intent_classifier 的裁决组件"
            ],
            "module": "intent_classifier"
          },
          "success": true,
          "target_module": "intent_classifier",
          "confidence": 0.96,
          "timestamp": "2026-05-22T15:25:45.772257"
        }
      ]
    },
    "parameter_extractor": {
      "context": [
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: parameter_extractor

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
尚未执行任何 capability

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:26:05.009593"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示参数提取失败：regex='往<方向>走' 在 '往前走' 中无匹配，回退到默认'前'。需要验证此失败是根因还是被回退机制掩盖。同时需确认 regex 设计本身是否存在缺陷导致无法正确解析'前'作为方向参数。"
}
```""",
          "timestamp": "2026-05-22T15:26:05.009620"
        },
        {
          "step": "log_localization_llm",
          "role": "user",
          "content": """## 任务

你是机器人控制 Pipeline 的日志分析专家。验证可疑模块是否存在问题，定位到具体的组件。

## 待验证模块

模块名称: parameter_extractor
模块置信度: 0.0
怀疑原因: 

模块内部组件: regex_match, capture, default_fallback

**你必须只分析这个模块，不要分析其他模块。**
**通过日志验证该模块是否确实存在问题，并定位到具体的组件。**

## 当前分支记忆

## 上下文

- [llm_decision] assistant: ```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示参数提取失败：regex='往<方向>走' 在 '往前走' 中无匹配，回退到默认'前'。需要验证此失败是根因还是被回退机制掩盖。同时需确认 regex 设计本身是否存在缺陷导致无法正确解析'前'作为方向参数。"
}
```


## 输入信息
情景描述: 用户说往前走但机器人没有反应
失败日志:

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 分析要求

1. **只分析上述待验证模块**：不要分析其他模块，即使日志中提到了其他模块
2. **分析该模块的日志表现**：查看该模块在日志中的输出
3. **定位问题组件**：找出该模块中具体哪个组件出了问题
4. **如果该模块没有问题**：返回 explanation = "verified_no_issue"
5. **计算置信度**：根据日志证据的充分性和问题定位的确定性，给出 confidence (0.0~1.0)

## 输出格式

必须返回纯 JSON 对象，禁止包含任何 Markdown 标记、思考过程或解释文字：

{
  "failed_module": "parameter_extractor",
  "failed_component": "该模块内的具体组件名称",
  "error_location": "日志中的具体错误位置",
  "explanation": "根本原因分析，或 'verified_no_issue' 表示该模块没有问题",
  "evidence": ["日志证据1", "日志证据2"],
  "confidence": 确认是当前模块出问题的置信度，0.0~1.0 的数字。
}

## Few-Shot 示例

### 示例 1：模块自身存在问题（独立根因）
输入：待验证模块=intent_classifier，模块组件=["classify", "ensemble", "裁决", "conf_threshold"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
[Ensemble] trace rule_fast: intent=2 conf=0.8
[Ensemble] trace llm_cmd: intent=1 conf=0.7
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
```
输出：
{
  "failed_module": "intent_classifier",
  "failed_component": "裁决",
  "error_location": "[IntentEnsemble] 裁决完成 intent=2 conf=0.42",
  "explanation": "裁决组件在 rule_fast(0.8)和 llm_cmd(0.7)都识别为指令时仍输出闲聊 intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'"
  ],
  "confidence": 0.9
}

### 示例 2：模块没有问题
输入：待验证模块=command_store，模块组件=["vector_search", "text_search", "merge_results"]
日志：
```
[CommandStoreClient] vector_search k=5 score=0.72
[CommandStoreClient] matched_command: {"id": "cmd_001"}
✅ ES搜索成功: 匹配 cmd_001
```
输出：
{
  "failed_module": "command_store",
  "failed_component": "N/A",
  "error_location": "N/A",
  "explanation": "verified_no_issue",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'"
  ],
  "confidence": 0.0
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入：待验证模块=parameter_extractor，模块组件=["regex_match", "capture", "default_fallback"]
日志：
```
[IntentEnsemble] 裁决完成 intent=2 conf=0.42
⚠️  裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"
```
输出：
{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex="往<方向>走" no match in "往前走"",
  "explanation": "regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。当前模块接收到的输入已被上游错误污染（'往前走'被误判为闲聊意图，导致 regex 模式不匹配）。这是上游错误导致的症状，不是 parameter_extractor 的独立根因。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游意图分类已误判",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 当前模块收到异常输入 → regex 匹配失败是症状而非独立缺陷"
  ],
  "confidence": 0.2
}

**重要**：示例3 中 confidence 为 0.2（低分），因为虽然当前模块确实出现了异常，但其异常可以被更上游的错误完全解释。根因是上游的 intent_classifier，不是当前模块。

## confidence 语义（必须严格遵守）

confidence 表示"**日志证据支持'问题根因在该模块'的置信度**"，不是"该模块工作质量评分"。

- 模块**确实有问题** → confidence 越高表示越确信（0.5~1.0）
- 模块**没有问题**（explanation = "verified_no_issue"）→ confidence **必须为 0.0**，表示"问题在该模块"的概率为零

严禁出现 explanation="verified_no_issue" 但 confidence > 0 的矛盾输出。

## 重要约束

1. **只输出 JSON**：禁止输出 ```json、```、解释文字或思考过程
2. **failed_module**：必须返回 "parameter_extractor"
3. **explanation**：如果该模块没有问题，必须返回 "verified_no_issue"，**此时 confidence 必须为 0.0**
4. **confidence**：必须返回 0.0~1.0 的数字，语义是"问题根因在该模块"的置信度
5. **evidence**：必须列出支撑 explanation 的具体日志证据，不能为空
5. **只分析待验证模块**：不要分析其他模块
6. **所有字段必须完整**：输出必须包含全部 5 个字段，缺一不可""",
          "timestamp": "2026-05-22T15:28:39.136529"
        },
        {
          "step": "log_localization_llm",
          "role": "assistant",
          "content": """{
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"",
  "explanation": "regex_match 组件的正则表达式模式存在设计缺陷，无法匹配'往前走'中的'前'作为方向参数。regex 模式'往<方向>走'设计为需要明确的方向词（如'左'、'右'）作为捕获组，但'前'作为修饰'走'的副词性方向词，无法被正确捕获。虽然 default_fallback 组件触发了回退机制使用默认值'前'，使最终结果碰巧正确，但 regex_match 组件的设计缺陷导致无法精确提取用户意图中的方向参数。该问题属于 parameter_extractor 模块内部的设计问题，而非上游污染或回退机制问题。",
  "evidence": [
    "日志行：'[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"' - regex_match 确实匹配失败",
    "日志行：'⚠️ 参数提取失败: 缺少\"方向\"参数，回退到默认\"前\"' - default_fallback 触发回退",
    "因果关系：regex 模式设计不当 → 无法识别'前'作为有效方向词 → 触发回退使用默认值"
  ],
  "confidence": 0.8
}""",
          "timestamp": "2026-05-22T15:28:39.136550"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: parameter_extractor

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: log_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:33:57.726514"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过 log_localization。当前 parameter_extractor 的正则匹配失败是符合预期的：输入'往前走'不包含具体方向词（'左'、'右'等），正则'往<方向>走'无法匹配是正确行为。模块回退到默认值'前'的逻辑也是合理的。参数提取器自身的处理流程正常，根因可能在上游（指令识别匹配到了不合适的 cmd_001）。置信度评估：约0.2，低于0.3阈值，当前模块基本可以排除。"
}""",
          "timestamp": "2026-05-22T15:33:57.726544"
        },
        {
          "step": "reflection_llm",
          "role": "user",
          "content": """## 任务

你是当前诊断分支的反思器。你的唯一任务是：基于分支内已执行的能力结果和失败日志，评估"问题根因是否属于当前分支的模块 parameter_extractor"。

你禁止执行任何其他判断，包括但不限于：
- 禁止决定是否发送通知
- 禁止决定整个诊断流程是否停止
- 禁止将其他模块指定为问题模块
- 禁止输出任何与 JSON 格式无关的思考过程或解释文字

## 当前分支模块

parameter_extractor

## 情景描述

用户说往前走但机器人没有反应

## 失败日志


# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 当前分支完整记忆（包含决策对话和能力执行结果）

## 上下文

- [llm_decision] assistant: ```json
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志显示参数提取失败：regex='往<方向>走' 在 '往前走' 中无匹配，回退到默认'前'。需要验证此失败是根因还是被回退机制掩盖。同时需确认 regex 设计本身是否存在缺陷导致无法正确解析'前'作为方向参数。"
}
```
- [log_localization_llm] assistant: {
  "failed_module": "parameter_extractor",
  "failed_component": "regex_match",
  "error_location": "[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"",
  "explanation": "regex_match 组件的正则表达式模式存在设计缺陷，无法匹配'往前走'中的'前'作为方向参数。regex 模式'往<方向>走'设计为需要明确的方向词（如'左'、'右'）作为捕获组，但'前'作为修饰'走'的副词性方向词，无法被正确捕获。虽然 default_fallback 组件触发了回退机制使用默认值'前'，使最终结果碰巧正确，但 regex_match 组件的设计缺陷导致无法精确提取用户意图中的方向参数。该问题属于 parameter_extractor 模块内部的设计问题，而非上游污染或回退机制问题。",
  "evidence": [
    "日志行：'[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"' - regex_match 确实匹配失败",
    "日志行：'⚠️ 参数提取失败: 缺少\"方向\"参数，回退到默认\"前\"' - default_fallback 触发回退",
    "因果关系：regex 模式设计不当 → 无法识别'前'作为有效方向词 → 触发回退使用默认值"
  ],
  "confidence": 0.8
}
- [llm_decision] assistant: {
  "capability": null,
  "end_signal": true,
  "reason": "已执行过 log_localization。当前 parameter_extractor 的正则匹配失败是符合预期的：输入'往前走'不包含具体方向词（'左'、'右'等），正则'往<方向>走'无法匹配是正确行为。模块回退到默认值'前'的逻辑也是合理的。参数提取器自身的处理流程正常，根因可能在上游（指令识别匹配到了不合适的 cmd_001）。置信度评估：约0.2，低于0.3阈值，当前模块基本可以排除。"
}

## 能力使用

- ✅ log_localization (confidence=0.80)
  failed_module: parameter_extractor
  explanation: regex_match 组件的正则表达式模式存在设计缺陷，无法匹配'往前走'中的'前'作为方向参数。regex 模式'往<方向>走'设计为需要明确的方向词（如'左'、'右'）作为捕获组，但'前'作为修饰'走'的副词性方向词，无法被正确捕获。虽然 default_fallback 组件触发了回退机制使用默认值'前'，使最终结果碰巧正确，但 regex_match 组件的设计缺陷导致无法精确提取用户意图中的方向参数。该问题属于 parameter_extractor 模块内部的设计问题，而非上游污染或回退机制问题。


## 反思要求

1. **评估问题归属**：基于上述完整分支记忆（包括 Agent 的决策过程、各能力执行结果、对话历史）和失败日志，严格评估"问题根因是否属于 parameter_extractor 模块"。判断时必须同时依据：
   - 失败日志中该模块的直接异常记录
   - 分支内已执行能力（如 log_localization / code_localization）对该模块的具体分析结论
   禁止仅凭直觉或情景描述打分，禁止考虑其他分支模块的表现。

2. **给出置信度**：confidence 必须是 0.0-1.0 的纯数字，表示"**问题根因在该模块**"的可信程度，不是"该模块工作质量评分"。
   - 模块确实有问题 → confidence 越高越确信（0.5~1.0）
   - 模块没有问题 → confidence 必须接近 0.0，表示"问题在该模块"的概率为零
   严禁出现"模块无问题"但 confidence 很高（如 >0.5）的矛盾输出。
   打分规则：
   - 0.90-1.00：日志和分支能力均明确指向该模块存在根因，证据链完整
   - 0.70-0.89：日志或分支能力有较强的指向性，但存在少量可解释的异常
   - 0.40-0.69：证据模棱两可，既有支持也有反驳的线索
   - 0.10-0.39：证据倾向于排除该模块，但无法完全确认
   - 0.00-0.09：日志和分支能力均表明该模块无问题，或问题已被充分排除

   **上下游影响规则（必须严格遵守）**：
   Pipeline 顺序：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。
   - 如果当前模块的异常可以被更上游模块的错误**完全解释**（如上游输出错误数据导致当前模块处理失败），则当前模块的 confidence 必须 ≤ 0.3，因为这是"症状"不是"根因"
   - 只有当当前模块存在**独立缺陷**（与上游无关）时，confidence 才应 ≥ 0.5
   - 严禁因为"当前模块确实报错了"就给高分——必须判断报错是自身缺陷还是上游污染导致的症状

3. **列出关键证据**：evidence 数组中的每条证据必须引用日志原文或能力执行结果中的具体信息（如具体的错误日志行、具体的组件名、具体的根因描述），禁止输出空洞的概括性语句（如"模块表现异常"、"日志显示有问题"）。

## 输出格式

必须且只返回一个纯 JSON 对象，禁止包含 Markdown 代码块标记（```json）、解释文字、思考过程或任何其他内容：

{
  "module": "parameter_extractor",
  "confidence": 0.0-1.0,
  "explanation": "为什么问题属于或不属于当前模块",
  "evidence": ["证据1", "证据2"]
}

## Few-Shot 示例

### 示例 1：确认当前模块有问题（独立根因）
输入分支模块：intent_classifier
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"，"⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令"
分支能力结论：log_localization 定位到 failed_component="裁决"

输出：
{
  "module": "intent_classifier",
  "confidence": 0.92,
  "explanation": "日志和分支能力均明确指向该模块的裁决组件：rule_fast(0.8)和llm_cmd(0.7)均识别为指令，但裁决仍输出闲聊intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'",
    "log_localization 结果：failed_component='裁决'"
  ]
}

### 示例 2：排除当前模块（自身无问题）
输入分支模块：command_store
日志证据："[CommandStoreClient] vector_search k=5 score=0.72"，"✅ ES搜索成功: 匹配 cmd_001"
分支能力结论：log_localization 返回 explanation="verified_no_issue"

输出：
{
  "module": "command_store",
  "confidence": 0.02,
  "explanation": "日志显示 vector_search 成功匹配 cmd_001，ES搜索无异常；log_localization 已验证该模块无问题",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'",
    "log_localization 结果：explanation='verified_no_issue'"
  ]
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入分支模块：parameter_extractor
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"（上游已误判），"[ParameterExtractor] regex='往<方向>走' no match in '往前走'"
分支能力结论：log_localization 定位到 regex_match 失败

输出：
{
  "module": "parameter_extractor",
  "confidence": 0.18,
  "explanation": "当前模块的 regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。上游错误导致当前模块接收到了异常输入，regex 匹配失败是症状而非独立根因。根因在上游 intent_classifier 的裁决组件。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游在阶段1已误判为闲聊",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 下游收到异常输入 → regex 匹配失败是症状，不是独立缺陷"
  ]
}

**重要**：示例3 中 confidence 为 0.18（低分），因为当前模块的异常可以被更上游的错误完全解释。严禁因为"当前模块确实出现了报错"就给高分。

## 重要约束

1. module 字段必须严格等于 "parameter_extractor"，任何其他值（包括大小写差异）均视为无效输出，会导致解析失败。
2. confidence 必须是 0.0 到 1.0 之间的纯数字，禁止使用字符串形式，禁止超出此范围。
3. 禁止输出 decision、final_module、stop、notify、action 等额外字段。
4. 禁止在 JSON 中输出"停止通知"、"跳过 Finalization"、"通知某某"等流程控制文字。
4. 只反思当前模块 parameter_extractor，禁止在 explanation 或 evidence 中与其他模块进行比较或引用其他模块的结论。
6. 禁止在 JSON 前后输出任何 ``` 标记或自然语言解释。""",
          "timestamp": "2026-05-22T15:34:21.248466"
        },
        {
          "step": "reflection_llm",
          "role": "assistant",
          "content": """{"module":"parameter_extractor","confidence":0.12,"explanation":"虽然log_localization指出regex_match组件存在设计缺陷，但该异常已被default_fallback机制成功处理：regex匹配失败触发回退到默认值'前'，最终协议生成成功获得'move_forward'模板。这表明parameter_extractor模块的内部处理逻辑是有效的——regex无法匹配是符合预期的（'前'确实是副词性方向词而非名词性方向词），回退机制正确补救了这一情况。分支最终的end_signal判断也确认'参数提取器自身的处理流程正常'。当前机器人无响应的直接原因是EMQXClient重试耗尽，与parameter_extractor模块无因果关系。","evidence":["日志行：'[ParameterExtractor] regex=\"往<方向>走\" no match in \"往前走\"' - regex_match匹配失败","日志行：'⚠️ 参数提取失败: 缺少\"方向\"参数，回退到默认\"前\"' - default_fallback成功补救","日志行：'[ProtocolBuilder] template=\"move_forward\" found, ✅ 协议生成成功' - 最终输出正确","log_localization结论：虽然指出regex设计缺陷，但同时确认'该问题属于parameter_extractor模块内部的设计问题，而非上游污染或回退机制问题'，且回退机制本身工作正常","llm_decision最终判断：confidence=0.2, '参数提取器自身的处理流程正常，根因可能在上游（指令识别匹配到了不合适的 cmd_001）'"]}""",
          "timestamp": "2026-05-22T15:34:21.248471"
        }
      ],
      "iteration": [
        {
          "step": "log_localization",
          "evidence": {
            "failed_module": "parameter_extractor",
            "failed_component": "regex_match",
            "error_location": """[ParameterExtractor] regex="往<方向>走" no match in "往前走"""",
            "explanation": "regex_match 组件的正则表达式模式存在设计缺陷，无法匹配'往前走'中的'前'作为方向参数。regex 模式'往<方向>走'设计为需要明确的方向词（如'左'、'右'）作为捕获组，但'前'作为修饰'走'的副词性方向词，无法被正确捕获。虽然 default_fallback 组件触发了回退机制使用默认值'前'，使最终结果碰巧正确，但 regex_match 组件的设计缺陷导致无法精确提取用户意图中的方向参数。该问题属于 parameter_extractor 模块内部的设计问题，而非上游污染或回退机制问题。",
            "evidence": [
              """日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - regex_match 确实匹配失败""",
              """日志行：'⚠️ 参数提取失败: 缺少"方向"参数，回退到默认"前"' - default_fallback 触发回退""",
              "因果关系：regex 模式设计不当 → 无法识别'前'作为有效方向词 → 触发回退使用默认值"
            ],
            "confidence": 0.8
          },
          "success": true,
          "target_module": "parameter_extractor",
          "confidence": 0.8,
          "timestamp": "2026-05-22T15:28:39.136562"
        },
        {
          "step": "reflection",
          "evidence": {
            "confidence": 0.12,
            "explanation": "虽然log_localization指出regex_match组件存在设计缺陷，但该异常已被default_fallback机制成功处理：regex匹配失败触发回退到默认值'前'，最终协议生成成功获得'move_forward'模板。这表明parameter_extractor模块的内部处理逻辑是有效的——regex无法匹配是符合预期的（'前'确实是副词性方向词而非名词性方向词），回退机制正确补救了这一情况。分支最终的end_signal判断也确认'参数提取器自身的处理流程正常'。当前机器人无响应的直接原因是EMQXClient重试耗尽，与parameter_extractor模块无因果关系。",
            "evidence": [
              """日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - regex_match匹配失败""",
              """日志行：'⚠️ 参数提取失败: 缺少"方向"参数，回退到默认"前"' - default_fallback成功补救""",
              """日志行：'[ProtocolBuilder] template="move_forward" found, ✅ 协议生成成功' - 最终输出正确""",
              "log_localization结论：虽然指出regex设计缺陷，但同时确认'该问题属于parameter_extractor模块内部的设计问题，而非上游污染或回退机制问题'，且回退机制本身工作正常",
              "llm_decision最终判断：confidence=0.2, '参数提取器自身的处理流程正常，根因可能在上游（指令识别匹配到了不合适的 cmd_001）'"
            ],
            "module": "parameter_extractor"
          },
          "success": true,
          "target_module": "parameter_extractor",
          "confidence": 0.12,
          "timestamp": "2026-05-22T15:34:21.248474"
        }
      ]
    },
    "emqx_client": {
      "context": [
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: emqx_client

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
尚未执行任何 capability

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:34:25.257702"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """```json
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志显示 MQTT 发布后设备无响应，最终失败点落在 emqx_client，但日志只呈现'设备无响应'的症状，没有暴露代码逻辑层面的明确错误。参数提取虽有回退，但协议仍成功生成，当前模块自身未报异常。置信度在 0.3~0.5 之间证据不充分，需要 code_localization 深入代码逻辑，验证 emqx_client 在连接建立、消息发送、回调等待等环节是否存在独立缺陷（如超时配置不合理、重试策略问题、topic 配置错误等）。"
}
```""",
          "timestamp": "2026-05-22T15:34:25.257725"
        },
        {
          "step": "llm_decision",
          "role": "user",
          "content": """你是 DebugAgent 诊断分支中的「分支规划专家」。

你的任务不是诊断问题本身，而是决定当前诊断分支下一步是否还需要执行 capability，或者是否应该结束当前分支并进入 Reflection。

## 当前分支信息
- branch_module: emqx_client

## 情景描述
用户说往前走但机器人没有反应

## 失败日志摘要

# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 已执行的 capabilities
已执行: code_localization

## 置信度语义（严格遵守）

当前分支的置信度表示「**当前模块是否为问题根因模块**」的判断强度，不是"工作质量评分"，也不是"继续分析的必要性"。

- 0.0：可以确定「当前模块不是问题根因模块」
- 1.0：可以确定「当前模块就是问题根因模块」
- 越接近 0，越倾向于排除当前模块
- 越接近 1，越倾向于确认当前模块
- 越接近 0.5，表示当前证据仍然不充分，无法判断

**严禁出现以下矛盾：**
- 模块日志显示工作正常、explanation="verified_no_issue"，但 confidence > 0.3
- 模块被上游错误污染导致异常，但 confidence 打高分（应打低分，因为根因在上游）

## 可用 capabilities

当前分支内允许使用以下两种 capability，每次只能选一个：

- **log_localization**：从失败日志中验证当前模块是否存在异常，并定位当前模块内部可能的问题点。
  - 适用场景：日志中已经能看到明显的错误堆栈、异常输出、或模块行为矛盾，证据比较清晰
  - 优点：快，基于日志直接判断
  - 局限：当日志只显示症状、没有暴露代码逻辑本身时，可能无法定位根因

- **code_localization**：调用 Opencode（已配置 gitnexus MCP 工具）对当前模块的代码进行系统级分析。
  - 适用场景：日志证据模糊、只看到结果值不对、或需要看代码才能确认根本原因（如早期返回逻辑错误、正则匹配顺序问题、权重配置问题等）
  - 优点：能深入代码逻辑，找到日志看不出的根因
  - 局限：慢（需要 300-600 秒），只在必要时使用

## 上下游影响分析

Pipeline 各模块按严格顺序执行：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。上游模块的输出直接作为下游模块的输入。

**关键原则**：
- 上游错误会导致下游模块出现**症状性异常**（如下游收到错误输入后报错），但根因在上游不在下游
- 当当前模块的异常可以被更上游的错误完全解释时，confidence 必须打低分（≤0.3），因为根因不在当前模块
- 当前模块自身有独立缺陷（与上游无关）时，confidence 才应打高分（≥0.7）

## 决策规则

1. 每个诊断分支**至少**要执行一次能力验证（log_localization 或 code_localization）。
2. **优先选择 log_localization**：
   - 如果日志中已有明确的错误信息、异常堆栈、或模块行为矛盾 → 选 log_localization
3. **log_localization 之后，根据 confidence 决定下一步**：
   - last_confidence <= 0.3：基本可以排除当前模块，结束分支
   - last_confidence > 0.3 且 < 0.5：证据不够充分，需要 code_localization 深入代码确认
   - last_confidence >= 0.5：怀疑程度较高，为了进一步定位到代码级根因，推荐执行 code_localization 进行深层确认
   - 如果日志中没有直接暴露代码逻辑错误，只有症状（如"返回值不对"但不知道哪行代码导致的）→ 即使 confidence 不高也应选 code_localization
4. **不要重复执行同一种 capability**。
5. **code_localization 最多执行一次**。
6. 只要当前证据已经比较明确地支持「确认当前模块」或「排除当前模块」，就应该结束当前分支。
7. 不允许因为"还想更确定"而重复执行已经执行过的 capability。
8. 如果没有可执行的新 capability，必须 end_signal=true。

## 输出格式

必须只返回纯 JSON 对象，禁止包含 Markdown、解释文字或代码块。

当需要继续执行 capability 时，输出示例：

示例1（优先用日志定位）：
{
  "capability": "log_localization",
  "end_signal": false,
  "reason": "日志中已出现该模块的异常输出，先用 log_localization 验证"
}

示例2（日志模糊，需要代码定位）：
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志只显示症状但无明确代码错误，需要 code_localization 深入代码逻辑"
}

当应该结束当前分支时，输出：

{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过定位能力，当前证据已足够支持确认或排除当前模块，结束分支进入 Reflection"
}""",
          "timestamp": "2026-05-22T15:41:08.948491"
        },
        {
          "step": "llm_decision",
          "role": "assistant",
          "content": """{
  "capability": null,
  "end_signal": true,
  "reason": "已执行过 code_localization，日志中设备无响应可能是上游协议生成或设备本身问题，EMQXClient 模块作为通信层已正确执行发送流程，根因大概率不在当前模块，结束分支进入 Reflection 汇总各分支诊断结果"
}""",
          "timestamp": "2026-05-22T15:41:08.948514"
        },
        {
          "step": "reflection_llm",
          "role": "user",
          "content": """## 任务

你是当前诊断分支的反思器。你的唯一任务是：基于分支内已执行的能力结果和失败日志，评估"问题根因是否属于当前分支的模块 emqx_client"。

你禁止执行任何其他判断，包括但不限于：
- 禁止决定是否发送通知
- 禁止决定整个诊断流程是否停止
- 禁止将其他模块指定为问题模块
- 禁止输出任何与 JSON 格式无关的思考过程或解释文字

## 当前分支模块

emqx_client

## 情景描述

用户说往前走但机器人没有反应

## 失败日志


# === 阶段1: 意图分类 ===
[IntentEnsemble] 裁决完成 intent=2 conf=0.42 cost=127ms
[Ensemble] trace rule_fast: intent=2 conf=0.8 cost=1ms reasons=['chat_pattern']
[Ensemble] trace llm_light: intent=1 conf=0.65 cost=120ms reasons=['llm_light']
[Ensemble] trace llm_cmd: intent=1 conf=0.7 cost=115ms reasons=['llm_cmd']
⚠️  裁决失败: 意图=2(闲聊) 但 rule_fast+llm_cmd 都命中指令

# === 阶段2: 指令重写 (旁路) ===
[InstructionRewriter] rewrite: "往前走"
✅ 改写成功: "往前走" → "往前走"

# === 阶段3: ES双搜 ===
[CommandStoreClient] vector_search k=5 score=0.72
✅ ES搜索成功: 匹配 cmd_001

# === 阶段4: 参数提取 ===
[ParameterExtractor] regex="往<方向>走" no match in "往前走"
⚠️  参数提取失败: 缺少"方向"参数，回退到默认"前"

# === 阶段5: 协议生成 ===
[ProtocolBuilder] template="move_forward" found
✅ 协议生成成功

# === 阶段6: MQTT发送 ===
[EMQXClient] publishing to topic: robot/robot_001/control
[EMQXClient] wait_for_callback timeout=5000ms
[EMQXClient] ❌ 重试耗尽，设备无响应
        

## 当前分支完整记忆（包含决策对话和能力执行结果）

## 上下文

- [llm_decision] assistant: ```json
{
  "capability": "code_localization",
  "end_signal": false,
  "reason": "日志显示 MQTT 发布后设备无响应，最终失败点落在 emqx_client，但日志只呈现'设备无响应'的症状，没有暴露代码逻辑层面的明确错误。参数提取虽有回退，但协议仍成功生成，当前模块自身未报异常。置信度在 0.3~0.5 之间证据不充分，需要 code_localization 深入代码逻辑，验证 emqx_client 在连接建立、消息发送、回调等待等环节是否存在独立缺陷（如超时配置不合理、重试策略问题、topic 配置错误等）。"
}
```
- [llm_decision] assistant: {
  "capability": null,
  "end_signal": true,
  "reason": "已执行过 code_localization，日志中设备无响应可能是上游协议生成或设备本身问题，EMQXClient 模块作为通信层已正确执行发送流程，根因大概率不在当前模块，结束分支进入 Reflection 汇总各分支诊断结果"
}

## 能力使用

- ✅ code_localization (confidence=0.90)
  module: emqx_client
  explanation: 在 emqx_client 模块中发现严重的 publish 超时处理缺陷：代码调用 msg.wait_for_publish(timeout=0.3s) 等待 QoS 1 的 PUBACK 确认，但当该调用超时时，paho-mqtt 不会抛出异常，代码继续检查 msg.rc（这是消息入队时的返回值，仍为 MQTT_ERR_SUCCESS），导致函数错误地返回 {"published": True}。随后系统继续等待设备回调（wait_for_callback timeout=5000ms），但消息实际上可能从未送达设备，设备自然不会响应，最终表现为'重试耗尽，设备无响应'。0.3秒的超时配置对于 MQTT 往返确认过于严苛，进一步放大了该缺陷的触发概率。


## 反思要求

1. **评估问题归属**：基于上述完整分支记忆（包括 Agent 的决策过程、各能力执行结果、对话历史）和失败日志，严格评估"问题根因是否属于 emqx_client 模块"。判断时必须同时依据：
   - 失败日志中该模块的直接异常记录
   - 分支内已执行能力（如 log_localization / code_localization）对该模块的具体分析结论
   禁止仅凭直觉或情景描述打分，禁止考虑其他分支模块的表现。

2. **给出置信度**：confidence 必须是 0.0-1.0 的纯数字，表示"**问题根因在该模块**"的可信程度，不是"该模块工作质量评分"。
   - 模块确实有问题 → confidence 越高越确信（0.5~1.0）
   - 模块没有问题 → confidence 必须接近 0.0，表示"问题在该模块"的概率为零
   严禁出现"模块无问题"但 confidence 很高（如 >0.5）的矛盾输出。
   打分规则：
   - 0.90-1.00：日志和分支能力均明确指向该模块存在根因，证据链完整
   - 0.70-0.89：日志或分支能力有较强的指向性，但存在少量可解释的异常
   - 0.40-0.69：证据模棱两可，既有支持也有反驳的线索
   - 0.10-0.39：证据倾向于排除该模块，但无法完全确认
   - 0.00-0.09：日志和分支能力均表明该模块无问题，或问题已被充分排除

   **上下游影响规则（必须严格遵守）**：
   Pipeline 顺序：rejection_classifier → intent_classifier → instruction_rewriter → command_store → parameter_extractor → protocol_builder → emqx_client。
   - 如果当前模块的异常可以被更上游模块的错误**完全解释**（如上游输出错误数据导致当前模块处理失败），则当前模块的 confidence 必须 ≤ 0.3，因为这是"症状"不是"根因"
   - 只有当当前模块存在**独立缺陷**（与上游无关）时，confidence 才应 ≥ 0.5
   - 严禁因为"当前模块确实报错了"就给高分——必须判断报错是自身缺陷还是上游污染导致的症状

3. **列出关键证据**：evidence 数组中的每条证据必须引用日志原文或能力执行结果中的具体信息（如具体的错误日志行、具体的组件名、具体的根因描述），禁止输出空洞的概括性语句（如"模块表现异常"、"日志显示有问题"）。

## 输出格式

必须且只返回一个纯 JSON 对象，禁止包含 Markdown 代码块标记（```json）、解释文字、思考过程或任何其他内容：

{
  "module": "emqx_client",
  "confidence": 0.0-1.0,
  "explanation": "为什么问题属于或不属于当前模块",
  "evidence": ["证据1", "证据2"]
}

## Few-Shot 示例

### 示例 1：确认当前模块有问题（独立根因）
输入分支模块：intent_classifier
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"，"⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令"
分支能力结论：log_localization 定位到 failed_component="裁决"

输出：
{
  "module": "intent_classifier",
  "confidence": 0.92,
  "explanation": "日志和分支能力均明确指向该模块的裁决组件：rule_fast(0.8)和llm_cmd(0.7)均识别为指令，但裁决仍输出闲聊intent=2",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42'",
    "日志行：'⚠️ 裁决失败: 意图=2 但 rule_fast+llm_cmd 都命中指令'",
    "log_localization 结果：failed_component='裁决'"
  ]
}

### 示例 2：排除当前模块（自身无问题）
输入分支模块：command_store
日志证据："[CommandStoreClient] vector_search k=5 score=0.72"，"✅ ES搜索成功: 匹配 cmd_001"
分支能力结论：log_localization 返回 explanation="verified_no_issue"

输出：
{
  "module": "command_store",
  "confidence": 0.02,
  "explanation": "日志显示 vector_search 成功匹配 cmd_001，ES搜索无异常；log_localization 已验证该模块无问题",
  "evidence": [
    "日志行：'[CommandStoreClient] vector_search k=5 score=0.72'",
    "日志行：'✅ ES搜索成功: 匹配 cmd_001'",
    "log_localization 结果：explanation='verified_no_issue'"
  ]
}

### 示例 3：上游污染导致下游症状（非根因模块）
输入分支模块：parameter_extractor
日志证据："[IntentEnsemble] 裁决完成 intent=2 conf=0.42"（上游已误判），"[ParameterExtractor] regex='往<方向>走' no match in '往前走'"
分支能力结论：log_localization 定位到 regex_match 失败

输出：
{
  "module": "parameter_extractor",
  "confidence": 0.18,
  "explanation": "当前模块的 regex_match 确实匹配失败，但该异常发生在上游 intent_classifier 将指令误判为闲聊之后。上游错误导致当前模块接收到了异常输入，regex 匹配失败是症状而非独立根因。根因在上游 intent_classifier 的裁决组件。",
  "evidence": [
    "日志行：'[IntentEnsemble] 裁决完成 intent=2 conf=0.42' - 上游在阶段1已误判为闲聊",
    "日志行：'[ParameterExtractor] regex="往<方向>走" no match in "往前走"' - 匹配失败确实发生",
    "因果关系：上游误判 → 下游收到异常输入 → regex 匹配失败是症状，不是独立缺陷"
  ]
}

**重要**：示例3 中 confidence 为 0.18（低分），因为当前模块的异常可以被更上游的错误完全解释。严禁因为"当前模块确实出现了报错"就给高分。

## 重要约束

1. module 字段必须严格等于 "emqx_client"，任何其他值（包括大小写差异）均视为无效输出，会导致解析失败。
2. confidence 必须是 0.0 到 1.0 之间的纯数字，禁止使用字符串形式，禁止超出此范围。
3. 禁止输出 decision、final_module、stop、notify、action 等额外字段。
4. 禁止在 JSON 中输出"停止通知"、"跳过 Finalization"、"通知某某"等流程控制文字。
4. 只反思当前模块 emqx_client，禁止在 explanation 或 evidence 中与其他模块进行比较或引用其他模块的结论。
6. 禁止在 JSON 前后输出任何 ``` 标记或自然语言解释。""",
          "timestamp": "2026-05-22T15:42:26.290302"
        },
        {
          "step": "reflection_llm",
          "role": "assistant",
          "content": """{
  "module": "emqx_client",
  "confidence": 0.92,
  "explanation": "code_localization 能力已深入代码逻辑，发现 emqx_client 模块存在独立的 publish 超时处理缺陷。具体为：代码调用 wait_for_publish(timeout=0.3s) 等待 QoS 1 的 PUBACK 确认，但超时时不抛异常，继续检查 msg.rc 得到 MQTT_ERR_SUCCESS，导致错误返回 published=True；随后 wait_for_callback timeout=5000ms 但消息实际未送达设备，最终表现为'重试耗尽，设备无响应'。0.3秒超时对 MQTT 往返确认过于严苛。该缺陷位于 emqx_client 自身代码逻辑，与上游模块（protocol_builder 等）无关，上游协议已成功生成 move_forward，参数也已回退为'前'，问题根因在 emqx_client 的超时处理和错误返回机制。",
  "evidence": [
    "日志行：'[EMQXClient] publishing to topic: robot/robot_001/control' 和 '[EMQXClient] wait_for_callback timeout=5000ms'",
    "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应'",
    "code_localization 结论：'在 emqx_client 模块中发现严重的 publish 超时处理缺陷：代码调用 msg.wait_for_publish(timeout=0.3s) 等待 QoS 1 的 PUBACK 确认，但当该调用超时时，paho-mqtt 不会抛出异常，代码继续检查 msg.rc（这是消息入队时的返回值，仍为 MQTT_ERR_SUCCESS），导致函数错误地返回 {\"published\": True}'",
    "code_localization 结论：'0.3秒的超时配置对于 MQTT 往返确认过于严苛，进一步放大了该缺陷的触发概率'",
    "日志行：'[ProtocolBuilder] template=\"move_forward\" found' 和 '✅ 协议生成成功' - 证明上游协议生成无问题，问题在 emqx_client 自身"
  ]
}""",
          "timestamp": "2026-05-22T15:42:26.290329"
        }
      ],
      "iteration": [
        {
          "step": "code_localization",
          "evidence": {
            "module": "emqx_client",
            "confidence": 0.9,
            "explanation": """在 emqx_client 模块中发现严重的 publish 超时处理缺陷：代码调用 msg.wait_for_publish(timeout=0.3s) 等待 QoS 1 的 PUBACK 确认，但当该调用超时时，paho-mqtt 不会抛出异常，代码继续检查 msg.rc（这是消息入队时的返回值，仍为 MQTT_ERR_SUCCESS），导致函数错误地返回 {"published": True}。随后系统继续等待设备回调（wait_for_callback timeout=5000ms），但消息实际上可能从未送达设备，设备自然不会响应，最终表现为'重试耗尽，设备无响应'。0.3秒的超时配置对于 MQTT 往返确认过于严苛，进一步放大了该缺陷的触发概率。""",
            "evidence": [
              {
                "file_path": "super_agent/integrations/emqx.py",
                "line_numbers": [
                  192,
                  196,
                  204,
                  206
                ],
                "code_snippets": [
                  """publish_wait_timeout = float(settings.get("publish_wait_timeout_seconds", 0.3))""",
                  """msg = runtime.client.publish(session.command_topic, payload=json.dumps(payload, ensure_ascii=False), qos=settings["qos"], retain=retain)""",
                  "msg.wait_for_publish(timeout=publish_wait_timeout)",
                  "rc = int(msg.rc)"
                ]
              },
              {
                "file_path": "config/config.yaml",
                "line_numbers": [
                  282
                ],
                "code_snippets": [
                  "publish_wait_timeout_seconds: 0.3"
                ]
              },
              {
                "file_path": "super_agent/integrations/emqx.py",
                "line_numbers": [
                  166,
                  170,
                  175
                ],
                "code_snippets": [
                  "with runtime.lock:",
                  "if not bool(runtime.client.is_connected()):",
                  """runtime.client.connect(settings["host"], settings["port"], keepalive=settings["keepalive"])"""
                ]
              }
            ]
          },
          "success": true,
          "target_module": "emqx_client",
          "confidence": 0.9,
          "timestamp": "2026-05-22T15:40:49.110976"
        },
        {
          "step": "reflection",
          "evidence": {
            "confidence": 0.92,
            "explanation": "code_localization 能力已深入代码逻辑，发现 emqx_client 模块存在独立的 publish 超时处理缺陷。具体为：代码调用 wait_for_publish(timeout=0.3s) 等待 QoS 1 的 PUBACK 确认，但超时时不抛异常，继续检查 msg.rc 得到 MQTT_ERR_SUCCESS，导致错误返回 published=True；随后 wait_for_callback timeout=5000ms 但消息实际未送达设备，最终表现为'重试耗尽，设备无响应'。0.3秒超时对 MQTT 往返确认过于严苛。该缺陷位于 emqx_client 自身代码逻辑，与上游模块（protocol_builder 等）无关，上游协议已成功生成 move_forward，参数也已回退为'前'，问题根因在 emqx_client 的超时处理和错误返回机制。",
            "evidence": [
              "日志行：'[EMQXClient] publishing to topic: robot/robot_001/control' 和 '[EMQXClient] wait_for_callback timeout=5000ms'",
              "日志行：'[EMQXClient] ❌ 重试耗尽，设备无响应'",
              """code_localization 结论：'在 emqx_client 模块中发现严重的 publish 超时处理缺陷：代码调用 msg.wait_for_publish(timeout=0.3s) 等待 QoS 1 的 PUBACK 确认，但当该调用超时时，paho-mqtt 不会抛出异常，代码继续检查 msg.rc（这是消息入队时的返回值，仍为 MQTT_ERR_SUCCESS），导致函数错误地返回 {"published": True}'""",
              "code_localization 结论：'0.3秒的超时配置对于 MQTT 往返确认过于严苛，进一步放大了该缺陷的触发概率'",
              """日志行：'[ProtocolBuilder] template="move_forward" found' 和 '✅ 协议生成成功' - 证明上游协议生成无问题，问题在 emqx_client 自身"""
            ],
            "module": "emqx_client"
          },
          "success": true,
          "target_module": "emqx_client",
          "confidence": 0.92,
          "timestamp": "2026-05-22T15:42:26.290341"
        }
      ]
    }
  },
  "bug_module": "intent_classifier",
  "owner_open_id": "ou_da6f41f726b64bd84672ca958390b282"
}